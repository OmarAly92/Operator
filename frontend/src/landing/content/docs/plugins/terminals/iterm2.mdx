---
title: iTerm2 integration
description: Legacy terminal-plugin URL; the current desktop terminal is built in.
---

The iTerm2/AppleScript terminal plugin and `opr open` command are not part of the Go rewrite. The current desktop uses an xterm surface over Operator's WebSocket [terminal mux](/docs/plugins/terminals/web).
