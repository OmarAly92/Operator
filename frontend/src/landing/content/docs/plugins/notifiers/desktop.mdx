---
title: Desktop notifications
description: Legacy notifier-plugin URL; current notifications are built into the Operator desktop app.
---

The standalone `desktop` notifier and `opr setup desktop` command are not supported by the Go rewrite. Operator now stores durable notification records in the daemon and shows them through the [notification center](/docs/plugins/notifiers/dashboard), with Electron toasts while the app is running.
