---
title: Architecture
description: How the Go daemon, Tauri desktop app, storage, controllers, and adapters fit together.
---

Operator is a local, single-user system built around a long-running Go daemon. The Tauri/React desktop app, optional CLI, and Flutter mobile app are thin clients over the daemon's HTTP, SSE, and terminal interfaces.

## System Overview

```text
Tauri + React desktop ───┐
opr CLI ──────────────────┼─> Go daemon ─> services/managers ─> SQLite
Flutter mobile (opt-in LAN) ┘    │                 │
                                │                 ├─> agent/runtime/workspace adapters
                                ├─> SSE events    │
                                ├─> terminal mux  └─> GitHub SCM observer
                                └─> standalone browser runtime (agent-browser)
```

The desktop app discovers, launches, and supervises the daemon. The renderer uses a generated typed client rather than importing backend logic. The CLI is also a thin HTTP client; it never opens SQLite or launches agent adapters directly.

## Sessions And Worktrees

Workers in Git projects own isolated worktrees; workspace projects can materialize a root plus child-repository worktrees, while Scratch sessions use an Operator-managed directory. Every session runs the harness's native terminal UI inside a tmux runtime on macOS/Linux or a conpty host on Windows.

## Durable Facts, Derived Status

Operator stores durable facts rather than a display status. Important session facts include:

- Agent activity: active, idle, waiting for input, blocked, or exited.
- Whether the session has been terminated.
- The terminal runtime handle and launch generation.
- Pull request, check, and review-comment facts.

The service layer computes labels such as working, needs input, CI failed, or ready to merge when a client reads a session. This prevents stored status from drifting away from source facts.

Failed or unknown runtime probes are observations, not proof of death. Cleanup also refuses to force-delete a dirty registered worktree.

## Storage And Live Updates

Durable state is stored in SQLite under `~/.operator/data` by default. Database triggers append changes to a `change_log`; a CDC poller tails that log and broadcasts invalidations to in-process subscribers and `GET /api/v1/events`.

The event stream supports replay with `Last-Event-ID`. Clients use it to invalidate targeted queries rather than transferring every full resource on each update.

All application state remains under `~/.operator`, including the run file, managed worktrees, desktop webview data, and mobile configuration. `OPERATOR_DATA_DIR` and `OPERATOR_RUN_FILE` provide advanced overrides for daemon storage and discovery.

## Services And Adapters

HTTP controllers validate requests and call domain services. Services own read/write policy; session and lifecycle managers coordinate multi-step work. External systems sit behind Go port interfaces and leaf adapters.

Current adapters include more than twenty agent harnesses, tmux/conpty runtime support, git-worktree isolation, and GitHub SCM observation. Operator reuses installed provider binaries and their existing authentication. The dormant native Chat code remains only during its staged removal.

GitHub PR observation is shipped. The broader tracker lane is still incomplete, so historical claims that every tracker plugin participates in lifecycle automation should not be treated as current behavior.

## Pull Requests And Lifecycle Reactions

The SCM observer polls GitHub using lazy authentication, ETag guards, and semantic diffing. It writes pull-request facts to SQLite and notifies lifecycle logic when CI fails, reviews request changes, or merge conflicts appear.

The daemon also exposes pull-request merge and comment-resolution actions plus reviewer-agent routes. Desktop clients receive concise summaries rather than raw CI logs or complete review bodies.

## Network Boundaries

The primary daemon listener binds `127.0.0.1:3001` and is unauthenticated. It serves the desktop app and CLI and is never configurable to a public bind.

Connect Mobile creates a second listener only when explicitly enabled. It binds the LAN interface behind bearer-password authentication and serves the app API and authenticated terminal mux, but excludes shutdown, telemetry, mobile-control, and browser-control routes. Transport is plaintext and intended only for a trusted home network.

## Terminal Streaming And Browser Automation

`/mux` provides per-client attachment to the agent terminal and session-scoped worktree shells.

Browser automation is owned end to end by the daemon through the checksum-pinned `agent-browser` binary, which drives a per-session isolated Chromium profile under the Operator state root. Browser control is blocked on the mobile LAN listener.

## Load-Bearing Rules

1. Never store display status; derive it from durable facts.
2. Never treat failed probes as proof that a session died.
3. Never force-delete dirty worktrees.
4. Keep all app state under `~/.operator` unless an explicit supported override applies.
5. Keep the primary listener on loopback; only Connect Mobile may create the authenticated opt-in LAN listener.
6. Keep the CLI and UI thin; domain logic belongs in the daemon.
7. Emit durable change events through SQLite triggers and CDC.
8. Add new migrations; never modify an already-merged migration.

## Next

<Cards>
	<Card title="Dashboard" href="/docs/dashboard" description="See how these capabilities appear in the desktop app." />
	<Card title="Platforms" href="/docs/platforms" description="Understand desktop and mobile platform behavior." />
	<Card title="Troubleshooting" href="/docs/troubleshooting" description="Diagnose daemon, controller, terminal, and SCM problems." />
</Cards>
