# SDD ledger — plan: /Users/omaraly/development/AI/Operator-tauri/docs/superpowers/plans/2026-08-20-tauri-port.md

Initial planning commit: ca97133a7. Merge base: 96c90c52d44202a91eadd206e67bf3bc9c429f5d.

Authoritative planning files were copied byte-for-byte from the dirty source checkout into ca97133a7; no unrelated source changes were transferred.

Spec: docs/superpowers/specs/2026-08-16-tauri-port-design.md. The plan has a reachable binding spec.

## Preflight cross-task and interface scan

| Tasks | Shared file or interface | Finding |
|---|---|---|
| 1 → 2 | `frontend/package.json`, performance schema/results, benchmark document | Task 1 defines contract; Task 2 supplies Electron evidence. Compatible. |
| 1 → 8 | parity ledger | Task 8 updates entries after bridge ownership moves. Compatible. |
| 1 → 12–18, 20–21 | parity ledger | Later tasks replace dispositions and Task 20 closes rows before Task 21 deletion. Compatible. |
| 2 → 4 | terminal benchmark runner and performance results | Task 2 creates Electron measurement runner; Task 4 extends it for Tauri terminal acknowledgements. Compatible. |
| 2 → 6, 19, 22 | benchmark document/results | Baseline feeds kill gate, optimization comparison, and final report. Compatible. |
| 3 → 4 | `src-tauri`, package scripts | Task 3 scaffold permits Task 4 native webview measurement. Compatible. |
| 3 → 6 | Tauri config, Phase 0 workflow | Task 6 packages the Task 3 scaffold. Compatible. |
| 3 → 7, 12–14, 17, 20 | Rust crate/lib/config/capabilities | Task 3 creates foundation; later tasks extend it. Compatible. |
| 3 → 9, 11 | daemon config/origin contract | Task 3 CORS additions retain Go config ownership for later routes. Compatible. |
| 4 → 6 | terminal evidence and result schema | Task 6 consumes Task 4 measurements. Compatible. |
| 5 → 6 | Phase 0 workflow and browser evidence | Task 6 consumes Task 5 standalone-browser results. Compatible. |
| 6 → 7–22 | mechanical Phase 0 decision | Explicit hard dependency. Task 7 may start only on `continue` or `linux-canvas`. |
| 7 → 8 | Rust lib and daemon status bridge contract | Task 8 exposes Task 7 commands through shell-neutral bridge. Compatible. |
| 7 → 11–14, 17, 20 | Rust app lifecycle and resource roots | Later native modules build on supervisor/state-root foundation. Compatible. |
| 8 → 9–11, 13–17 | `tauri-bridge`, shared bridge types | Task 8 establishes contract; downstream implementations fill it. Compatible. |
| 9 → 10 | settings service, migration 0088, API | Task 10 imports legacy settings into Task 9 singleton. Compatible. |
| 9 → 13, 17 | settings API | Native shortcuts and updater read/write settings. Compatible. |
| 10 → 21 | Electron local scan removal | Task 10 ports scan controls before deletion. Compatible. |
| 11 → 21 | Electron telemetry bootstrap removal | Task 11 ports daemon bootstrap before deletion. Compatible. |
| 12 → 18, 22 | app marker, executable/artifact identity | Packaging and docs preserve Task 12 contract. Compatible. |
| 13 → 14 | capabilities, native bridge, parity ledger | Task 14 adds native grants without broadening Task 13 permissions. Compatible. |
| 14 → 16 | opener and bridge contract | Task 16 uses the HTTP(S)-validated opener for external preview. Compatible. |
| 15 → 16, 21 | browser service/controller API | Task 16 preserves standalone API; Task 21 deletes Electron broker only after it works. Compatible. |
| 16 → 21 | deferred Browser panel record and renderer removals | Task 16 removes only documented behavior; Task 21 removes now-dead Electron code. Compatible. |
| 17 → 18 | updater state/channel and Tauri config | Task 18 packages feeds for Task 17 updater. Compatible. |
| 18 → 21–22 | release scripts/workflows/artifacts | Electron deletion and docs depend on migrated release verification. Compatible. |
| 19 → 22 | performance results/baseline document | Task 22 reports Task 19 measurements. Compatible. |
| 20 → 21 | Tauri E2E and parity ledger | Task 21 gated by Task 20 evidence. Compatible. |
| 21 → 22 | desktop build surface and documentation | Task 22 documents verified Electron-free system. Compatible. |

| Task | Internal coherence check | Finding |
|---|---|---|
| 1 | inventory tests, ledger checker, benchmark contract | Compatible. |
| 2 | result schema, runners, renderer marks, native evidence | Compatible; cross-platform results are intentionally deferred to native runners. |
| 3 | pinned scaffold, CORS tests, state audit | Compatible; all-platform audit belongs in workflow evidence. |
| 4 | production terminal component, benchmark-only entry, matrix | Compatible. |
| 5 | mocked policy tests and native agent-browser probe | Compatible. |
| 6 | decision tool only accepts complete all-platform evidence | Compatible and intentionally gates later work. |
| 7 | supervisor contracts and packaged sidecar discovery | Compatible. |
| 8 | coexistence bridge with Browser namespace deferred | Compatible. |
| 9 | singleton migration/settings APIs/sqlc/API regeneration | Compatible. |
| 10 | one-time import and LAN-blocked developer routes | Compatible. |
| 11 | loopback bootstrap and LAN block | Compatible. |
| 12 | marker preservation, relocation, CLI discovery | Compatible. |
| 13 | native behavior, narrow permissions, persisted settings | Compatible. |
| 14 | native integrations, safe staged drops, narrow permissions | Compatible. |
| 15 | standalone adapter and existing public browser API | Compatible. |
| 16 | revision acknowledgement, external opener, panel-only removal | Compatible. |
| 17 | state machine, pinned updater, state-root constraint | Compatible. |
| 18 | feeds/artifacts/native update verification | Compatible but requires external native runners for final evidence. |
| 19 | reports before measured renderer changes | Compatible. |
| 20 | E2E-only embedded driver and production absence proof | Compatible. |
| 21 | absence test before removal and retained sidecars | Compatible. |
| 22 | verified documentation and complete native matrix | Compatible. |

No preflight contradictions found. No rulings required.

## Tasks

- Task 1: complete
- Task 2: implementation/review complete; native evidence pending
- Task 3: implementation/review complete; native evidence pending
- Task 4: implementation/review complete; native evidence pending
- Task 5: complete
- Task 6: complete (decision stop-port — Phase 0 gate closed, Task 7 gated)
- Task 7: complete
- Task 8: complete
- Task 9: complete (commits 03a8e6755..c79baba8d, review clean)
- Task 10: complete (commits c79baba8d..d852af7dc, review clean)
- Task 11: complete (commits d852af7dc..b001739ac, review clean)
- Task 12: complete (commit 1cdba476f; remediation re-reviewed clean by fresh full-surface adversarial review after relocation-lock fix round; see Session 2026-08-23 block below)
- Task 13: pending
- Task 14: pending
- Task 15: pending
- Task 16: pending
- Task 17: pending
- Task 18: pending
- Task 19: pending
- Task 20: pending
- Task 21: pending
- Task 22: pending

Task 1: dispatched (base ca97133a78c7b350ed49f4146da23599c8e16116, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-1-brief.md)
Task 1: fix round 1/5 (3 addressed, 0 open — fixed exception allowlist, TypeScript-safe inventory, exact metadata keys; commits d09651b79..8311fc600)
Task 1: complete (commits ca97133a7..8311fc600, review clean)
Task 2: dispatched (base 8311fc6004cefc1146dc1ac2b13413cb801c835b, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-2-brief.md)
Ruling: Task 2 publisher identity attestation requires explicit trusted macOS Team ID, Windows certificate identity/thumbprint, and Linux GPG fingerprint inputs from authorized native release runners; the repository and release history contain only secret placeholders and no values. The harness must fail closed when they are absent and may not accept an arbitrary cryptographically valid signer. Cost if wrong: a legitimate future signed artifact needs runner configuration before evidence can be collected, but no untrusted artifact can be recorded as binding evidence.
Task 2: fix round 1/5 (6 addressed, 0 open; commits 491b16ba4..3b6631911)
Task 2: fix round 2/5 (5 addressed, 0 open; commits 3b6631911..da8f34a18)
Task 2: fix round 3/5 (3 addressed, 0 open; commits da8f34a18..778448caf)
Task 2: implementation/review complete; native evidence pending (commits 8311fc600..778448caf; review clean; Windows/Linux/macOS signed-release evidence remains an external Phase 0 runner gate, not complete)
Task 3: dispatched (base 778448caf7c3158c90d425dce7b25306f72e6a9a, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-3-brief.md)
Ruling: Task 3 may add frontend/scripts/audit-tauri-state.test.mjs to test the required audit behavior; the plan lists the production audit script but omits its test file, while TDD and the state-location kill gate require a committed regression test. Cost if wrong: one adjacent test file is added, but without it the audit cannot be trusted against the observed macOS protected-path failure.
Ruling: Task 3 retains the declared macOS 10.15 minimum and records minimum-version state evidence as missing because only a macOS 14 hosted runner is available; macOS 14 results must not be presented as proof for 10.15. Cost if wrong: Phase 0 may stop-port until a minimum-version runner supplies the required evidence, but weakening the target or claiming unsupported evidence would violate the spec.
Task 3: fix round 1/5 (4 addressed, 0 open; commits 4c8df305c..18618c24e)
Task 3: implementation/review complete; native evidence pending (commits 778448caf..18618c24e; review clean; Windows/Linux/macOS 10.15 evidence remains an external Phase 0 gate, not complete)
Task 4: dispatched (base 18618c24e0480a06b8cf3dd0964734b47a31dda8, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-4-brief.md)
Task 4: implementation complete (commit 751744d15); review/fix status and native matrix evidence pending (Windows/Linux, signed/installed, vtebench, and visual-correctness evidence remain unproduced)
Ruling: Task 3 may modify backend/internal/httpd/cors.go in addition to the listed cors_test.go because the required exact-origin and hostile-lookalike behavior cannot be implemented through tests alone; the current wildcard localhost implementation must be narrowed surgically. Cost if wrong: a small extra production diff is added to Task 3, but leaving it unchanged would violate the binding origin boundary.
Ruling: Task 3 may update frontend/package-lock.json when pinning the explicitly required Tauri npm CLI/API versions because npm ci requires the lockfile to match package.json. Cost if wrong: one generated lockfile diff is added to the scaffold, but leaving it stale would break reproducible CI installs.
Tasks 1–4 broad review: fix wave dispatched from 751744d15 for terminal correctness, benchmark provenance/accounting, state-audit observation, and contract/documentation accuracy.
Ruling: The contract-docs brief item 5 ("do not create replacement comments") conflicts with the repo's binding zero-findings golangci-lint revive/exported gate (backend/.golangci.yml:57), which requires a doc comment on every exported symbol. The brief's intent was to strip Task 3's unauthorized five-line explanatory rewrite, not to remove the mandatory doc comment. Ruling: restore a minimal one-line `// DefaultAllowedOrigins ...` doc comment in house style; the explanatory rewrite stays removed. Cost if wrong: one doc line remains that a strict reading of the no-comments rule would delete, but CI would otherwise block the merge.
Tasks 1–4 broad review: fix round 1/5 (12 addressed, 0 open — terminal workload-ack race, benchmark provenance binding, state-audit transient observation, evidence-status honesty, idle-memory daemon split, fixed 120×40 grid, recovery-render ack, native runtime identity command, credential rejection, unmocked harness test, browser-docs truth, deferred-row reclassification lock; commits 751744d15..a3c8a7e75; scoped re-review clean, no new Critical/Important breakage)
Task 1–4: minor (deferred): benchmark-artifact.mjs scenarioConfiguration block indentation inconsistent (frontend/scripts/benchmark-artifact.mjs:490-502)
Task 1–4: minor (deferred): committed fix-contract-docs-report.md lagged the doc-comment ruling; resolved by committing the fix note with this round's bookkeeping
Task 4: implementation/review complete; native matrix evidence pending (commits 18618c24e..a3c8a7e75 including shared fix wave, review clean)
Task 5: dispatched (base 193bf9eab, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-5-brief.md)
Ruling: Subagent implementer dispatches for Task 5 failed repeatedly (three empty/lost results, zero work landed across two checkouts verified). User explicitly directed the controller to implement Task 5 directly, suspending the fresh-implementer rule for this task only. Independent task review after completion remains mandatory. Cost if wrong: controller-authored code skips the usual implementer self-review; mitigated by the unchanged independent reviewer gate.
Task 5: fix round 1/5 (5 addressed, 1 open — truncate byte/char mismatch, path prefix bypass, locate Set handling, partial-download test, walkFiles swallow; scan env remains; commits 817887b98..76e4aeb51; scoped re-review 5/6 addressed)
Task 5: fix round 2/5 (1 addressed, 0 open — scan absolute path for POSIX; commits 76e4aeb51..89717e949; scoped re-review clean, no new Critical/Important breakage)
Task 5: complete (commits 193bf9eab..89717e949, review clean)
Task 5: minor (deferred): createSessionRoot returns {root} wrapper object (frontend/scripts/agent-browser-phase0.mjs:302)
Task 5: minor (deferred): workflow node-version 24 not exactly pinned (.github/workflows/tauri-phase0.yml:67)
Task 5: minor (deferred): LOCALAPPDATA set but APPDATA not mirrored on win32 (frontend/scripts/agent-browser-phase0.mjs:220)
Task 5: minor (deferred): scan regex tab vs whitespace for PowerShell (frontend/scripts/agent-browser-phase0.mjs:427)
Task 6: dispatched (base 89717e949, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-6-brief.md)
Task 6: fix round 1/5 (7 addressed, 0 open — daemon no-op, bridge hard-coded, || true masking, terminal-open gate, dead updater logic, synthetic evidence, leak check outside repo; commits 33ea6db85..034602f05; scoped re-review clean, no new Critical/Important breakage, minor loopholes remain)
Task 6: complete (commits 89717e949..034602f05, review clean, decision stop-port — Phase 0 gate closed, Task 7 must not start until native matrix provides continue/linux-canvas)
Task 7: dispatched (base 034602f05, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-7-brief.md)
Task 7: fix round 1/5 (7 addressed, 0 open — attached_owner stub, wait_for_ready drain, start_lock race, shell_env, Windows process_alive, weak oracles, dead code; commits 25926efc2..dbf594bcc; scoped re-review clean, no new Critical/Important breakage, minor pending leak/try_lock noted)
Task 7: complete (commits 034602f05..dbf594bcc, review clean)
Task 8: dispatched (base dbf594bcc, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-8-brief.md)
Task 8: fix round 1/5 (4 addressed, 1 open — production proxy Tauri resolution, import-scan dedup, dead createElectronBridge variant, compile-time ownership tests; shared→main browser-type import remains; commits bfc8a9e0c..7d9ebddb2)
Task 8: fix round 2/5 (1 addressed, 0 open — browser payload types moved to shared/browser-view-types.ts with main re-export shim; commits 7d9ebddb2..03a8e6755; scoped re-review clean, no new Critical/Important breakage)
Task 8: complete (commits dbf594bcc..03a8e6755, review clean)
Ruling: User explicitly directed to continue the implementation plan after the Phase 0 merge despite the stop-port gate, overriding the Global Constraint "Do not begin Task 7 unless Task 6 records continue or linux-canvas" for Tasks 7 onward. Native evidence gaps remain deferred to CI; independent review stays mandatory per task. Cost if wrong: later tasks build on darwin-only evidence; a native-matrix failure may force rework or drop-platform.
Task 9: dispatched (base 03a8e6755, brief /Users/omaraly/development/AI/Operator-tauri/.superpowers/sdd/2026-08-20-tauri-port/task-9-brief.md)
Task 9: NOT STARTED — three subagent implementer dispatches returned empty with zero work landed (no commits, no report file, no migration 0088). Next controller: re-dispatch a fresh implementer OR implement directly per the Task 5 precedent ruling. Key context for the implementer is in the handoff prompt; the brief lists all files and steps.
Ruling: User explicitly directed to continue the implementation plan after the merge despite the Phase 0 stop-port gate, overriding the Global Constraint "Do not begin Task 7 unless Task 6 records continue or linux-canvas" for this continuation only. Independent review remains mandatory for each subsequent task. Cost if wrong: Task 7+ builds on unfalsifiable darwin-only evidence; a later native-matrix failure will require rework or a drop-platform decision, but stalling now would block the user's requested progress.
Ruling: Task 9 settings mutations emit no change_log CDC rows - app_settings has no project_id (change_log.project_id NOT NULL REFERENCES projects), the event_type CHECK allowlist cannot be extended without rebuilding change_log and every trigger (the codebase avoided exactly this in 0066), and a sentinel-project row would pollute mobile per-project sync streams with semantically wrong events. Settings consumers refetch via GET /api/v1/settings; the brief's "CDC trigger output" test case asserts settings mutations produce no change_log rows. Cost if wrong: a future cross-client settings-push feature needs the change_log rebuild done then, but no wrong rows are written meanwhile.
Ruling: the four new Task 9 endpoints resolve to PATCH /api/v1/settings/ui, /api/v1/settings/updates, /api/v1/settings/keybindings, /api/v1/settings/migration - the plan's shorthand "/updates, /keybindings, /migration" is relative to /api/v1/settings per spec line 163 ("The settings API remains under /api/v1/settings"). Cost if wrong: route paths move, breaking the bridge methods written against them; caught by Task 10+ integration.
Task 9: re-dispatched (base 03a8e6755, fresh implementer after three empty dispatches; brief task-9-brief.md, report task-9-report.md)
Task 9: implementer DONE_WITH_CONCERNS (commit c79baba8d; sqlc rebrand drift regenerated + consumer updated; npm run api broken at root, pinned npx used; settings_wiring.go added beyond brief list; transitions = vocabulary validation; bridge set-methods throw on daemon errors)
Task 9: review clean (spec compliant, quality approved, 0 Critical/Important). Controller resolved reviewer warnings: golangci-lint not installed locally - go vet clean on touched packages, CI lint remains external gate as for Tasks 1-8; settings routes inherit existing /api/v1/settings registration so LAN/mobile parity holds per spec line 163; Task 10 consumption of MarkLegacyDesktopImported/migration endpoint carried into Task 10 dispatch.
Task 9: minor (deferred): tauri-bridge REST-backed paths have zero behavioral tests (no tauri-bridge.test.ts; fallback-default mapping and throw-on-error setters untested) - add small vitest suite stubbing apiClient
Task 9: minor (deferred): getMigration silently falls back to pending on daemon failure - a declined decision can re-present the prompt during a transient outage (tauri-bridge.ts)
Task 9: minor (deferred): openapi.yaml carries duplicate MigrationState + SettingsMigrationState schemas for one concept (wire DTO vs service request body in build.go reqBody registration)
Task 9: minor (deferred): implementer report Modified list omitted app_settings_store.go (108 lines changed) though its test was listed under Created
Task 9: minor (deferred): pre-existing full-suite flakes (kilocode/opencode auth-probe timeouts) attributed to untouched adapters - controller note, not this branch
Task 9: complete (commits 03a8e6755..c79baba8d, review clean)
Task 10: implementer crashed mid-task on API network error (ENOTFOUND); resumed same agent with intact context and uncommitted files; completed commit d852af7dc + report
Task 10: review clean (spec compliant, quality approved, 0 Critical/Important; port fidelity verified line-by-line against import-folder-scan.ts; crash-resume tree verified complete)
Task 10: minor (deferred): scan.go byte-wise sort.Slice replaces JS localeCompare - non-ASCII repo names can order differently than desktop (disclosed, ASCII-only impact)
Task 10: minor (deferred): workspace mode computes ancestor warning before os.ReadDir - nonexistent root wastes up to a 5s git probe before the read error (same observable end state)
Task 10: minor (deferred): report self-review point 4 wording imprecise about mapLimited error behavior - documentation nit, no code defect
Task 10: minor (deferred): preferences.go len(chord.Key)==0 -> chord.Key=="" out-of-brief touch to a Task 9 file (semantically identical, required for lint gate, disclosed)
Task 10: minor (deferred): scan_test.go runtime.GOOS=="js" skip guard is unreachable dead weight in backend test binary
Task 10: complete (commits c79baba8d..d852af7dc, review clean)
Task 11: review clean (spec compliant, quality approved, 0 Critical/Important; nil-DisabledEvents footgun caught in self-review and pinned by raw-byte test; install-ID sharing proven via disk equality)
Task 11: minor (deferred): lan_listener_test.go LAN 404 case POSTs a GET-only route - prefix block proven, but a GET alongside would pin closer to real renderer traffic
Task 11: minor (deferred): tauri-bridge parseTelemetryBootstrap casts platform unchecked into NodeJS.Platform union - daemon only emits darwin/win32/passthrough today, cosmetic
Task 11: minor (deferred): pre-existing 1/34 cargo flake (shell-process 800ms deadlines) and 13 pristine-tree vitest failures in scripts/ + src/landing/** - controller notes, not this branch
Task 11: complete (commits d852af7dc..b001739ac, review clean)
Task 12: controller pre-review bounce - implementer disclosed relocate hard-deleting strictly-older bundle vs Electron trashing it; resumed implementer to replace delete with Trash move before review (correctness/fidelity concern)
Task 12: fix round 0/pre-review (1 addressed - Trash move via trashed_bundle_destination, Finder uniquification, decline-not-delete fallbacks; commits b83270dfc..89a753f93 amended; cargo test --lib 63 passed)

## Session 2026-08-23 — remediation continuation

Resumed from HANDOFF-NEXT-SESSION.md at HEAD 89a753f93 with ~64 files of UNCOMMITTED remediation (+4349/−877). All prior "review clean" claims are void per controller instruction; fresh independent reviewers required. Tree preserved as-found; gen/ and target/ never staged.
Ruling (session): historical ledger completion lines for Tasks 1–11 remain as commit records only; they are NOT accepted as review evidence. Every domain gets a fresh independent review against the current combined tree before its remediation commit. Cost if wrong: duplicate review effort, but no stale verdict can mask an unfixed defect again.
Dispatched three fresh READ-ONLY agents on the uncommitted tree: report-domainA-rereview.md (Go/services combined confirmation), report-domainB-audit.md (7 Phase 0 blockers vs interrupted second-round edits), report-domainC-audit.md (Rust tree compile/clippy/tests + permissions/ ACL + interruption damage). Implementation dispatches stay sequential and start only after the owning audit lands.
Domain C audit complete: 12/14 remediation claims verified against live code; cargo fmt/clippy/test (81 passed)/typecheck all green first run; no /Applications test leftovers. Open gaps: (a) NO cross-process relocation lock — Important, must fix before Task 12 close; (b) no automated Cargo.toml↔package.json version guard — Minor; (c) CFBundleName not checked (CFBundleExecutable covers identity) — Minor; permissions/autogenerated recommended for COMMIT (matches build.rs commands 1:1); tauri-bridge still references handler-less tray/opener commands — phase-0 scope question for Task 13+.
Domain B audit complete (report-domainB-audit.md): B3/B4 fully addressed; B1/B2/B5/B6/B7 partial with precise gap map; all 147 node --test tests pass; no crash-grade interruption damage; three NEW tee-pipeline masking regressions introduced mid-round (yml:73,76,152,164). Implementer brief written: review-fix-phase0-round2-brief.md (6 work items W1–W6).
Domain B implementer dispatched (opus, fresh agent) on brief review-fix-phase0-round2-brief.md; owns frontend/scripts/** + tauri-phase0.yml + docs/benchmarks/tauri-port-baseline.md + perf/** only; uncommitted contract; report → review-fix-phase0-round2-report.md.
Domain A fresh re-review complete (report-domainA-rereview.md): SPEC compliant YES / QUALITY approved NO. All three late fixes verified present and test-pinned; cross-contamination clean; race suite green. ONE CRITICAL: C1 uncommitted cors.go:31,47 exact-match tightening broke per-session preview origins opr-preview.<id>.localhost (CORS runs before previewOriginMiddleware) → 8 committed controller tests 403 ORIGIN_FORBIDDEN incl. masked symlink-escape test; dev-renderer origin lost too.
Task A ruling: the exact-origin CORS tightening stands (Phase 0 boundary mandate); preview serving must be restored WITHOUT reverting to the loopback wildcard — exempt validated preview-origin requests ahead of the CORS boundary (reorder or Host-matched preview-subdomain admission). Cost if wrong: either the old wildcard hole returns or previews break again; both directions pinned by cors_test.go hostile cases + the 8 controller tests.
Task A: minor (deferred): M1 case-insensitive-FS casing aliases bypass strictComparablePath containment on APFS (scan.go:403-416, pre-existing, loopback-only surface)
Task A: minor (deferred): M2 crashed install-ID publication strands .telemetry_install_id.<uuid>.tmp candidates (posthog.go:555-577)
Domain A fix implementer dispatched (opus, fresh agent) for C1; owns backend/internal/httpd/** only.
Task A: fix round 1/5 (C1 addressed — previewOriginMiddleware reordered ahead of corsMiddleware in NewRouterWithControl, router.go:61-62; preview hosts grammar-gated to opr-preview.<session>.localhost GET/HEAD only; new cors_test pinning test; all 8 controller tests unmasked incl. unmasked symlink-escape rejection on merits; scoped re-review verdict C1 ADDRESSED with full request-class matrix clean).
Task A ruling: hostile-Origin GET/HEAD executing the read-only preview handler is NOT a defect — method gate precedes work, zero mutation, no ACAO so unreadable cross-origin, exposure delta vs HEAD effectively zero (no-cors embedding carried no Origin at HEAD too). Advisory one-line Origin-equality hardening parked as deferred minor. Cost if wrong: a hostile local process can fetch loopback-only preview bytes it could equally curl directly under the unauthenticated-loopback hard rule.
Task A: fix round 2/5 dispatched to same implementer (scoped re-review NEW Important finding): dev-shell origins rejected by default violates spec line 63 "plus the existing trusted Electron development origin" — Electron dev-spawn env (main.ts:664-669) and Tauri dev (devUrl 127.0.0.1:5173) must stamp their dev origin explicitly; production defaults stay exactly three packaged origins. Ownership extended this round only: backend/internal/config/**, frontend/src/main.ts dev-spawn env, src-tauri daemon env stamping site, tauri.conf.json if required.
Task A: fix round 2 complete — both shells stamp their own dev origin at spawn (Electron daemonEnv() dev block + shared/shell-env.ts pure helper; Rust allowed_origins_override parses build.devUrl from tauri.conf.json, inserted only when unpackaged AND user value blank); config.go untouched; gates green (go build/vet/httpd+config suites, cargo fmt/clippy/test 83 passed, typecheck, vitest shell-env 25/25).
Task A: fix round 2 scoped re-review — DEV-ORIGIN FINDING: ADDRESSED, APPROVED. Adversarial URL-parsing probes fail closed; packaged path asserts key absent; defaults pin intact; no production-surface widening. Deferred minors recorded below.
Task A: minor (deferred): unpackaged runs REPLACE the three defaults with the stamped dev origin rather than unioning — escape hatch is explicit OPERATOR_ALLOWED_ORIGINS if a packaged client ever targets a dev daemon
Task A: minor (deferred): include_str! bakes devUrl at compile time (cannot diverge from webview URL within one binary — informational)
Task A: minor (deferred): advisory hostile-Origin one-line equality hardening in PreviewOrigin (controllers/sessions.go:437)
Task A: minor (deferred): pre-existing OPERATOR_ALLOWED_ORIGINS=" " yields empty allowlist (fail-closed narrowing, predates remediation)
Task A: complete pending domain commit (commits will be <new>; SPEC compliant YES per fresh combined-tree re-review; rounds 1–2 findings all ADDRESSED and APPROVED; M1/M2 + four new minors parked deferred)
Task A: complete (commit 3dd775a83 "fix(daemon): harden desktop migration and services", 29 files +1099/−177, review clean after fresh combined-tree re-review + two verified fix rounds). Generated artifacts regenerated byte-stable pre-commit (sqlc v1.31.1, api:spec, openapi-typescript@7.4.4). Round-1 CORS preview fix rides in this commit; round-2 dev-origin stamping (main.ts/shell-env*/supervisor.rs/tests.rs) intentionally rides with the Domain C commit since those are shell files.
Domain C implementer dispatched (opus, fresh agent) on brief review-domainC-fix-brief.md: W1 cross-process relocation lock (flock POSIX / LockFileEx-or-equivalent Windows, contended decline, crash-safe release), W2 Cargo↔package.json version-coupling test, W3 optional CFBundleName check; owns relocation.rs + test modules only; supervisor.rs dev-origin additions declared untouchable.
Domain C fix DONE (report-domainC-fix.md): RelocationLock try_acquire non-blocking (flock LOCK_EX|LOCK_NB POSIX; std share_mode(0) Windows — LockFileEx rejected because OVERLAPPED feature absent from windows-sys enablement), lock acquired before inspect/decide and carried by type through execute_relocation; contended → silent Continue; killed-holder release proven via child process exit(0); 4-thread full-flow serialization test over real FS bundles. CFBundleName=="Operator" added to bundle validation (RED captured). Version-coupling guard parses real manifests read-only + fixture-proven divergence detection. Gate: fmt/clippy clean, cargo test 92 passed (+9), build clean, concurrency reruns x3 green. Residual: Windows branch uncompiled locally (std-only API choice); cfg(macos) contended-decline glue integration-untested.
Fresh full-surface Domain C adversarial reviewer dispatched (opus) on review-domainC-postfix.diff (~212KB incl. permissions/ contents).
Domain C final adversarial review COMPLETE (report-domainC-finalreview.md): SPEC compliant YES / QUALITY APPROVED / ZERO Critical-Important. Lock guard provably unbypassable; supervision/state-root/JSON/ACL/versions verified with citations; cargo test 92 passed, fmt/clippy/build/typecheck green. Both requested re-verdicts ruled non-blocking by the reviewer.
Domain B implementer DONE_WITH_CONCERNS: new files phase0-platform-summary.mjs/.test.mjs + phase0-legacy-exercise.mjs; 174 tests green (+27 RED→GREEN); decision run verbatim stop-port exit 1 naming each missing evidence file; workflow gains pipefail at tee sites, rpm grep fix, app+zip+dmg verification, terminal-benchmark job, per-OS platform-summary jobs, decision-before-hard-gates upload. Concerns adjudicated by controller: (1) CORS evidence producer MISSING → fix round 2 dispatched to same implementer (workflow/script-level daemon probe, no backend edits); (2) observed-byte bounds [configured, configured+64KiB] accepted as sound given PTY echo; (3) fail-closed under unconfigured trust anchor matches the Task 2 trusted-signer ruling — correct behavior; (4) Tauri artifacts genuinely absent so summaries stay blocked → honest stop-port preserved.
Domain C final adversarial review COMPLETE (report-domainC-finalreview.md): SPEC compliant YES / QUALITY APPROVED / ZERO Critical-Important. Lock guard provably unbypassable (sole acquire site before inspect/decide, type-enforced carriage into execute_relocation); supervision/state-root/JSON/ACL/versions verified with citations; cargo fmt/clippy/build/typecheck green, cargo test 92 passed, no load-sensitive failure. Both requested re-verdicts ruled non-blocking.
Task C ruling: audit/benchmark command grants remaining in main-window capability are NOT a defect — generate_handler registers only daemon_* commands outside those modes so invocations fail closed; a static capability file cannot vary per launch mode and handler registration is the real dynamic gate. Cost if wrong: a future handler-registration widening without capability review could expose audit/benchmark commands to the renderer; mitigated by narrow per-command TOMLs making any such change visible.
Task C ruling: cfg(macos) contended-decline integration gap is an evidence gap, not a defect — every mechanism beneath it unit-tested incl. cross-process contention and killed-holder kernel release; discharge by running the packaged real-daemon relocation decision gate once on macOS after commit.
Task C: minor (deferred): shutdown-during-startup-attach epoch window can swallow a quit (orphaned owner=app daemon recovered next launch)
Task C: minor (deferred): orphaned .Operator.app.operator-stage-<uuid> staging dirs never swept after mid-relocation process death
Task C: minor (deferred): resolved_state_root() latent trap if called post-HOME-mutation without OPERATOR_DATA_DIR/RUN_FILE overrides
Task C: gitignore added for src-tauri target/ and gen/ (never-stage rule made structural); permissions/autogenerated committed per dual-reviewer recommendation.
Task C: complete (commit 1cdba476f "fix(tauri): correct native supervision and relocation", 28 files +2936/−445 incl. permissions/autogenerated/*.toml, src-tauri/.gitignore, and Domain-A-round-2 dev-origin stamping files main.ts/shell-env*)
Domain B round 2 interrupted by API 429 rate-limit kill mid-CORS-probe debugging (last state: fixing //readyz double-slash URL bug in phase0-cors-probe.mjs); resuming same agent per Task 10 crash-resume precedent.
Domain B round 2 DONE (crash-resumed same agent): new frontend/scripts/phase0-cors-probe.mjs derives cors evidence purely from observed HTTP responses against the real committed daemon (9 probe targets = 3 packaged origins + null/*/non-loopback/two lookalikes/unlisted dev loopback; fail-closed on any error, unexpected header, missing Vary: Origin, or grant drift; the live-caught //readyz refusal IS the RED evidence); wired into existing terminal-benchmark job's build-daemon+readiness pattern; partial-evidence computability pinned by new decision test; 177 tests green (+3), YAML parses, local decision still exit-1 stop-port naming every missing file. Fresh security review of full Domain B surface dispatched (review-domainB-postround2.diff ~361KB).
Domain B security re-review verdict: SPEC NO / QUALITY NOT APPROVED — integrity architecture confirmed (fabrication residue ZERO: derivation-from-raw with refusals, genuine Ed25519/blake2b512 minisign verification, byte-bound provenance manifests, repo-pinned trust only, pipefail everywhere, honest-first decision upload, continue-from-partial structurally impossible; local stop-port exit 1 reproduced; 116 focused tests green) BUT 6 Important findings leave B2/B6/B7 partially closed and the pipeline incapable of its own success path:
Task B Important #1: updater material downloaded to raw/updater then deleted while producer gets nonexistent --updater-dir → platform-summary leg dead as wired
Task B Important #2: evidenceScope:"binding" unwritable (terminal hardcodes non-binding + throws on override; shell omits; artifact never sets)
Task B Important #3: cpu-time crashes both shells (call sites pass {cpuMs} without workloads)
Task B Important #4: cpu-time/active-memory renderer workloads unsupported in harness → timeout not measurement
Task B Important #5: benchmark-artifact.mjs invoked nowhere in CI; parseArtifactArguments still rejects non-electron (Tauri measurement paths missing)
Task B Important #6: windows-artifact-verification.json swept into results dir and refused by collector → win32 summary permanently refused
Task B: minor (deferred): validateBridgeHandoff trusts self-asserted booleans (latent-only; sole producer emits no handoff branch)
Task B ruling (Minor 8): legacy exercise digests hash bare CI fixtures while summary compares against attested release-artifact digests — structurally red until an authorized runner supplies real attested artifacts; comparison logic itself correct. Cost if wrong: bridge-handoff binding stays unreachable longer than necessary, but no untrusted artifact can bind.
Task B: minor (deferred): browser process command-line sanitization leaves wrapper binary repo/home path on dev machines; identityPreserved uses /readyz service-string proxy; versions.json from CLI args not measured binaries; concurrency-proof fields not required by consumers; dead scripts/fixtures/phase0-evidence-fixture.json referenced by nothing
Task B ruling (#2 fix design): binding evidence scope requires explicit validated OPERATOR_BENCH_EVIDENCE_SCOPE=binding stamped by CI ONLY after repo-pinned trust anchor is configured; default/local stays non-binding; invalid value hard-errors. Preserves honest stop-port today, makes continue reachable on authorized runners without weakening verification. Cost if wrong: a misconfigured CI job could stamp scope binding — mitigated because binding additionally requires digest+crypto verification against pinned anchors.
Task B: fix round 3/5 dispatched to same implementer with all six Importants verbatim + the scope ruling.
Task B: fix round 3 DONE — F1 updater layout assembled+copied before cleanup; F2 resolveEvidenceScope strict env switch (binding|non-binding, hard error on invalid, CI stamps via repo variables only, binding additionally gated on non-local build profile); F3 cpu-time call sites record workload-tagged snapshots via cpuDeltasFromIterationSnapshots (validator untouched); F4 real 200k/50k compute workloads in harness for cpu-time+active-memory; F5 benchmark-artifact accepts tauri end-to-end (AppImage self-extract, NSIS 7z, resources-map component layout, packaged-component verification) + per-OS measure step wired; F6 windows-authenticode known-type validation; latent deriveArtifactSection sync/promise bug fixed. Gates: 186/186 node tests across 8 suites, 22/22 harness vitest, tsc clean, YAML parses, local decision exit-1 stop-port.
Task B: minor (deferred): terminalOpen has no benchmark producer anywhere — summaries refuse on that named gap even when all else green (honest-red; same class as absent Tauri artifacts; native-runner work)
Round-3 scoped re-review dispatched (opus) with scope-switch-honesty hunt h1 as priority.
Task B round-3 re-review verdict: F1–F6 ALL ADDRESSED and test-pinned; scope-switch honesty PASS; Tauri artifact paths fail closed honestly; BUT round 3 introduced THREE NEW CRITICALS: N1 main.tsx:108 calls renamed-away workloadController → ReferenceError kills harness mount on both shells (tsc excludes perf/**, vitest bypasses main.tsx); N2 yml find-sweep moves acp-runtime package/node_modules JSONs into perf/results → darwin summary permanently refused; N3 linux compositing pairs write identical filename → second overwrites first → linux summary impossible.
Task B: minor (deferred): binding-vs-trust-anchor cross-check for terminal/shell-unattested paths is procedural-only under the env-switch ruling
Task B: minor (deferred): pull_request runs resolve vars against base repo so an upstream's scope choice reaches fork PRs (mitigated: artifact binding still requires attestation inputs)
Task B ruling (process): rounds 4–5 fresh-agent default overridden for round 4 — incumbent resumed with SURGICAL-ONLY constraint. Why: monotonic convergence across rounds 1–3 (147→174→177→186 tests), failure mode this round was breadth-of-touch so a context-reset agent raises blast radius, and no capability tier above the incumbent exists for a meaningful bump. Cost if wrong: one more round if it regresses again; breaker adjudication applies regardless at round 5.
Task B: fix round 4 DONE (surgical, 5 files): main.tsx call site corrected to scenarioController + mutation-proven static-graph pin in harness.test.tsx; bundle-json glob sweep replaced with explicit named copies (shell baselines + windows-artifact-verification.json + linux-rpm-signature.txt→rpm-signature.txt); terminalResultVariant extends result filenames with compositing-enabled|disabled so linux pairs coexist. Gates: 188/188 node tests across 8 suites, 23/23 harness vitest, tsc clean, YAML parses, local decision exit-1 stop-port.
Task B round-4 scoped re-review verdict: ALL ADDRESSED — CLEAN. N1 pin mutation-tested live against two rename classes; N2 copy paths traced end-to-end through artifact download layouts; N3 filenames content-keyed vs COMPOSITING_MODES exact match, no consumer parses old scheme.
Task B: minor (deferred): N1 static-graph pin is heuristic — misses bare function refs without call parens, optional calls, template-literal expressions, default-import binding checks (both realistic rename classes caught)
Task B: minor (deferred): raw/terminal json find-sweep retained in yml — same bug class as N2, safe only while every uploaded benchmark-terminal json stays in RESERVED_RESULT_NAMES
Task B: complete (commit 269535589 "fix(phase0): derive decisions from verified raw evidence", 29 files +5519/−519 incl. 8 new scripts; loop closed at round 4 with ALL ADDRESSED — CLEAN scoped re-review)

## Session 2026-08-23 — closure

All three remediation domains integrated as separate reviewed commits on codex/tauri-port:
3dd775a83 fix(daemon): harden desktop migration and services (Domain A; fresh combined-tree re-review SPEC YES, rounds 1–2 verified, generated artifacts byte-stable pre-commit)
1cdba476f fix(tauri): correct native supervision and relocation (Domain C; audit 12/14 + relocation-lock fix round + fresh adversarial review APPROVED; permissions/ committed; gen//target/ gitignored)
269535589 fix(phase0): derive decisions from verified raw evidence (Domain B; security review drove rounds 2–4; final scoped re-review ALL ADDRESSED — CLEAN)

Full cross-domain verification evidence (this tree, post-commits):
- git diff --check clean; working tree contains ONLY the ledger modification.
- node --test: 176/176 (handoff's 7 suites) + 12/12 phase0-platform-summary.test.mjs = 188 pass / 0 fail.
- check:desktop-parity: ledger covers 103 entries.
- phase0-decision.mjs: exit 1, stop-port, naming missing evidence files — HONEST result preserved; no native evidence fabricated.
- Focused vitest (harness, runtime, XtermTerminal, bridge, tauri-bridge, telemetry): 148 passed.
- npm run typecheck + npm run frontend:typecheck: clean.
- cargo fmt --check / clippy --locked --all-targets -D warnings / test --locked (92 passed) / build --locked: all green.
- go build/vet ./...: clean; focused suite all ok; -race all ok; FULL go test ./... exit 0 with ZERO failures this run (previously-flaky interactive-shell auth tests passed without retry).
- Generators byte-stable post-commit (sqlc v1.31.1, api:spec, openapi-typescript@7.4.4): only the ledger changed afterwards.
- golangci-lint binary not installed locally — remains an external CI gate exactly as for Tasks 1–11.
review-fix-shell-report.md created per handoff requirement.

Task 13 gate assessment: preconditions MET (remediation integrated into three commits, fully verified, Task 12 freshly and cleanly reviewed). Task 13 NOT started in this session — handoff scope ends at "prepare to start".
Remaining external gates before any Phase 0 decision change: real signed native artifacts + authorized-runner trust configuration (Phase 0 stays stop-port until then), Windows runner compile of the relocation-lock branch, packaged macOS relocation decision-gate run, golangci-lint CI leg.
Task 13: dispatched (base 2160a0f1c, fresh opus implementer, brief task-13-brief.md regenerated from plan). Scope: window overlay/fullscreen events, theme hints, menu actions, recording suppression, close-terminal enablement, eleven parity-ledger shortcut events; RED fake-registrar contract tests first; global-shortcut plugin pinned =2.3.2; minimal default.json; settings persisted through Go before native apply. Controller commits after clean review.
Task 13: implementer DONE_WITH_CONCERNS (window/menu/shortcuts/theme modules + 16 contract tests, 110 cargo tests green, bridge suites 26/26, gates green). Reviewer adjudications: focus-scoped shortcuts FAITHFUL (zero Electron globalShortcut usage; ten are before-input-event webContents listeners); devtools feature ships in release but matches Electron's unconditional DevTools menu role (parity, flag for release-security review).
Task 13 review verdict: SPEC ✅ / QUALITY NOT APPROVED. Important #1 empty-override resurrects defaults instead of disabling (shortcuts.rs:450-452 `_ => defaults` swallows Some([])); diverges from Electron shared/shortcuts.ts `overrides[id] ?? defaults` AND Go preferences.go documented unassigned contract; reachable via shipped KeyboardShortcutsSettingsDialog conflict flow; test at native_contract_tests.rs:208-222 pins wrong expectation. Important #2 native application-menu accelerators silently dropped (Electron hidden role menu main.ts:300-313 gave Windows Reload/zoom/fullscreen/devtools/edit accelerators; mac default menu lacks reload/zoom/devtools; Tauri builds no menu on win/linux) — in neither report concerns nor later task briefs.
Task 13 Ruling (#2 ownership): the native accelerator menu belongs to THIS task — plan Task 13 interface says "menu actions" and creates menu.rs; dropping keyboard accelerators is a functional regression no later task owns, so fix in-loop, do not defer. Cost if wrong: extra in-task scope; but deferring would strand a shipped-behavior loss with no owning task until someone notices broken Ctrl+R/F11.
Task 13 Ruling (chrome deferral, reviewer adjudication 2): DEFERRED as an explicit tracked decision, not silently — Electron's hidden/hiddenInset titlebar + titleBarOverlay vs Tauri fully-decorated window produces double chrome day one; fixing requires coordinated renderer drag-region migration (data-tauri-drag-region) best done as its own reviewed unit. Recorded as known user-visible divergence needing user sign-off before any release; DWM caption tint coherent only because native chrome currently exists. Cost if wrong: shipping with double titlebar if the deferral is forgotten — mitigated by this ledger entry and the finishing-review surfacing it.
Task 13: minor (deferred): mac last-window-close exits app vs Electron staying alive until tray (revisit Task 14)
Task 13: minor (deferred): recording state not cleared on shell blur (renderer self-corrects)
Task 13: minor (deferred): unused-by-JS global-shortcut register/unregister grants widen compromise surface (brief-mandated; drop later)
Task 13: minor (deferred): failed GET /settings at launch leaves shortcuts unarmed indefinitely; pre-apply defaults transiently mis-map overridden chords
Task 13: minor (deferred): Cargo.lock incidental transitive churn beyond added deps
Task 13: fix round 1 dispatched to same implementer: F1 empty-override disables (flip enshrining test); F2 port native accelerator menu all platforms (hidden role menu win/linux, mac default coverage incl. reload/zoom/devtools/edit) routed through existing host calls; F3 (in-scope, minor-promoted) theme_set("system") follows live OS scheme changes via ThemeChanged (Electron nativeTheme.on("updated") parity).
Task 13: fix round 1 DONE — Some(empty)⇒unassigned + flipped expectation + dedicated unregister-frees-key test; platform menu templates (Edit/View/Window + mac app submenu), install_app_menu hidden-but-functional on Windows / visible Linux / replaces default macOS, all accelerators through dispatch_menu_action; managed ThemeState re-syncs via ThemeChanged in system mode. Gates green; cargo test 115 passed (21 native_contract); bridge vitest 26/26. Implementer disclosed one uncaptured full-suite failure never reproducing across five captured runs (known daemon-timing load profile) — re-reviewer instructed isolated-rerun protocol. Round-1 scoped re-review dispatched with Windows hide_menu accelerator-reality hunt as h1.
Task 13 round-1 scoped re-review verdict: ALL ADDRESSED — CLEAN. F1 verified verbatim against Go preferences.go wording; F2 Windows reality proven by source-tracing muda hide_for_hwnd (SetMenu NULL only) + tauri app.rs accelerator hook iterating stashed menus regardless of visibility — same contract as Electron autoHideMenuBar; h3 precedence: RegisterHotKey pre-empts while armed, Ctrl+W reaches hidden close role only when disarmed — strictly at least as strong as Electron preventDefault split; cargo test 115/0 first run (transient did not reproduce).
Task 13: minor (deferred): no startup apply_window_background when stored mode is System on light-OS machines (dark until first theme event) — visual-only load-time surface
Task 13: minor (deferred): hidden Ctrl+Plus zoomIn chord from Electron menu.ts:32 not registered (+ requires Shift on some layouts)
Task 13: minor (deferred): non-mac template lacks Quit CmdOrCtrl+Q and Force Reload; mac lacks Hide/Services/Window>Zoom defaults (report compared vs Windows-only menu.ts; Linux-default-menu delta undisclosed until re-review)
Task 13: complete pending domain commit (rounds: initial + 1; SPEC ✅ both reviews; QUALITY approved after round-1 CLEAN; chrome-restyling deferral remains an explicit tracked user-visible divergence requiring sign-off before release)
Task 13: complete (commit 0b7f14845 "feat(tauri): port window menu and shortcuts", 20 files +2269/−21 incl. window/menu/shortcuts/native_contract_tests modules, capabilities/default.json, 8 new ACL TOMLs; review chain: task review SPEC✅/QUALITY❌ → fix round 1 → scoped re-review ALL ADDRESSED — CLEAN)
Task 14: dispatched (base 1c60dac16, fresh opus implementer, brief task-14-brief.md regenerated). Scope: directory chooser, HTTP(S)-validated opener, clipboard incl. Linux primary selection, notification/click/badge + attention/toast policy, tray attention/open-session (retires parked mac last-window-close minor), saveDroppedFile staging under <state-root>/terminal-drops with 64 MiB cap / sanitize / collision-resistant atomic writes / 7-day prune / refuse-delete-outside-owned-dir; pins dialog 2.7.2 opener 2.5.4 clipboard-manager 2.3.2 notification 2.3.3; RED fake-based tests first; controller commits after clean review.
Task 14: implementer interrupted by API 429 mid-task (five new module files + Cargo/lib.rs edits present, pre-RED-test); resumed same agent per crash-resume precedent.
Task 14: implementer DONE_WITH_CONCERNS (native/tray/notification_policy/dropped_files(+tests) modules, 12 ACL TOMLs, badge asset, plugins pinned exact + base64 0.22.1 + linux arboard 3.6.1; RED captured 38 failures across four suites → cargo test 155 passed; vitest 35/35; gates green). Concerns for reviewer adjudication: (1) toast click-to-focus lacks real OS delivery via pinned plugin (routing fake-tested only); (2) strict HTTP(S) opener breaks Report-a-Problem mailto destination; (3) attention-mechanics deltas (request_user_attention auto-clear, devBounce non-cancellable); (4) tray locale byte-wise Ord ordering; (5) dropped-file bytes cross IPC base64-encoded (+33% near cap); (6) Windows/Linux cross-compiles blocked by missing C toolchains — cfg-gated backends verified line-by-line vs vendored sources, first three-platform CI is the real proof.
Ruling (process, gateway instability): six consecutive agent kills with identical cloudflare-intercept errors on large-context review agents; full-surface Task-14 reviewer abandoned after partial progress (badge provenance confirmed byte-identical to Electron's; Electron opener allows mailto — key fidelity evidence banked). Review SPLIT into two fresh small-context sonnet reviewers: R1 security half (dropped-files suite, ACL minimality, pins, lib.rs regression safety, cargo gates, badge), R2 fidelity+bridge half (mailto severity, click-gap ruling, tray/notification policy fidelity, bridge contracts, test honesty, typecheck/vitest/parity gates). Cost if wrong: split reviews may miss cross-half interactions; mitigated by disjoint scopes covering all ten original adjudications and controller merging verdicts.
Task 14 R2 (fidelity half) verdict: FINDINGS REQUIRE FIX LOOP. Important #1 global strict opener breaks mailto in Report-a-Problem + ChatMarkdown links + terminal context menu (Electron allowlist includes mailto at external-open.ts:1; spec scopes HTTP(S)-only to preview targets at :184,186; Rust gate global at native.rs:21-40,126-133) — fix = split preview gate from general opener accepting http/https/mailto per isAllowedAppExternalURL, update pinned test tauri-bridge.test.ts:311. Important #2 notifications:click deferral acceptable ONLY as explicit ledger entry + named owner + pre-release sign-off (chrome-deferral precedent). Tray fidelity faithful incl. mac stay-alive now implemented (retires Task 13 parked minor); notification policy matches Electron main.ts:1667-1790; bridge contracts exact; vitest 35/35; typecheck+parity green.
Task 14 R2 minors: tray startup locale hardcoded "en" vs persisted value (transient pre-sync); devBounce lacks Electron 2s cancel (dev-only); toast icon omitted win/linux; RED counts off-by-one native 5→6 / notification_policy 8→9 (GREEN-phase additions).
Task 14 R1' (security half, completed by user-run fresh session after gateway kills): CLEAN — S1 dropped-files properties all PRESENT+TESTED except symlink-escape delete refusal PRESENT-UNTESTED (canonicalize+direct-child check in code, no symlink test in 14-suite); S2 ACL compliant; S3 pins compliant; S4 regression safety clean with line citations; S5 badge 16×16 PNG byte-identical to Electron main.ts:1758 data URL, wired as Linux tray overlay; S6 fmt/clippy/test 155-0/build all exit 0.
Task 14 merged review verdict: QUALITY NOT APPROVED pending fix round — two Importants.
Task 14 Ruling (notifications:click deferral): ACCEPTED as an explicit tracked release-gating deferral — real OS toast-click activation requires UNUserNotificationCenter/WinRT integration outside the pinned plugin; routing+channel are implemented and fake-tested. OWNER: unassigned follow-up (candidate: dedicated native-notification activation work alongside Task 20 E2E gates). MUST be resolved and signed off before any release build ships. Cost if wrong: notification clicks silently fail to focus the window in shipped builds if this entry is forgotten — surfaced here and in the finishing review.
Task 14: minor (deferred): tray startup locale hardcoded "en" instead of persisted ui-settings value
Task 14: minor (deferred): devBounce lacks Electron's 2s cancel (dev-only surface)
Task 14: minor (deferred): toast icon omitted on win/linux (Electron set one)
Task 14: minor (deferred): RED counts off-by-one for native/notification_policy suites (GREEN-phase additions, disclosure accuracy only)
Task 14: minor (deferred): symlink-escape delete refusal untested (code present; test-only addition entering fix round as strengthening, not a defect)
Task 14: fix round dispatched to same implementer: F1 split opener gates (general open_external accepts http/https/mailto mirroring Electron external-open.ts allowlist; strict HTTP(S) preserved as the named preview-target validator for Task 16; update tauri-bridge.test.ts:311 pin + add mailto/javascript:/file: cases); F2 test-only symlink-escape delete-refusal tests.
Task 14: fix round 1 DONE — is_allowed_app_external_url (http/https/mailto, WHATWG-aligned, case-insensitive) on general path; is_allowed_preview_url strict validator preserved for Task 16 (tests-only call sites); symlink suite added as load-bearing regression pins (existing canonicalize+direct-child gate already refused — no production change needed). Gates: cargo test 157/0, vitest 35/35, fmt/clippy/typecheck/parity green.
Task 14 round-1 scoped re-review verdict: CLEAN. Line-by-line allowlist parity vs Electron confirmed via node WHATWG cross-probe (literal-CRLF and backslash-authority cases stricter than Electron — safe direction); bridge pin revert-sensitive; symlink tests proven load-bearing by gate-removal thought experiment. New informational Low parked below.
Task 14: minor (deferred): http_s_authority accepts malformed hosts (spaces, forbidden chars) that WHATWG rejects — same-scheme string handed to OS/browser; unexploitable mirror-divergence
Task 14: complete pending domain commit (review chain: split security/fidelity halves after gateway instability + fix round 1 + scoped re-review CLEAN; RELEASE-GATING DEFERRAL outstanding: notifications:click real OS delivery needs UNUserNotificationCenter/WinRT follow-up before any release ships)
Task 14: complete (commit 782dfa909 "feat(tauri): port desktop native integrations"; completion recorded in ledger commit 9271ff7b0)

## Session 2026-08-24 — Tasks 15–22 continuation

Resumed from HANDOFF-TASKS-15-22.md at HEAD 9271ff7b0, tree clean. Preflight scan rows for Tasks 15–22 already recorded above; no new contradictions found against current HEAD.
Task 15: dispatched (base 9271ff7b0e364ad87879bbe0f64ca2adcf6c4fe3, fresh opus implementer, UNCOMMITTED contract, brief task-15-brief.md regenerated from plan)
Task 15: implementer DONE_WITH_CONCERNS (adapters/agentbrowser install/policy/runtime + process_alive_{unix,windows}, service/browser/runtime.go contract, 5 tracked files modified; RED→GREEN adapter 48/48, Browser suite 30, build/vet/gofmt/-race/full go suite green, real-binary fixture PASS on agent-browser 0.33.1). Concerns to reviewer: load-sensitive rerun protocol applied (17 captured reruns green), in-process-only install serialization, Windows paths uncompiled locally (standing caveat), stream-disable non-port + no Electron-field synthesis disclosed, panel-only actions 503 with public names preserved, extra process_alive files beyond brief list.
Task 15: review package review-task15.diff (4249 lines incl. untracked contents); opus reviewer dispatched for SPEC + quality verdicts.
Task 15 review verdict: SPEC ❌ / QUALITY NOT APPROVED — 0 Critical, 1 Important, 6 Minor. Strengths noted: clean contract seam, real-process tests, careful containment, live-binary fixture validation.
Task 15 Important #1: stateRoot() = filepath.Dir(DataDir) (runtime.go:820-821) escapes Operator-owned roots under OPERATOR_DATA_DIR override — override replaces DataDir wholesale (config.go resolveDataDir), so OPERATOR_DATA_DIR=/tmp/op puts browser-engine/browser-runtime at /tmp/*, outside any Operator root; repo's own e2e harness sets this override (internal/cli/e2e_test.go:92). Fix: explicit state root threaded through Options resolved from canonical ~/.operator logic or supplied by config.
Task 15: minor (deferred): timing-bounded assertions tighter than legal worst case (runtime_test.go:538 <3s, :812 <5s vs awaitExit up to 5s post-kill) — widen to ~8s
Task 15: minor (deferred): explanatory comments on unexported symbols (install.go locateManagedExecutable, policy.go inheritAllowedEnv/flattenEnv, runtime.go sessionCall/prepareSessionCommand + Options field comments, daemon.go browserTeardown/resolveAgentBrowserBinary, test helpers)
Task 15: minor (deferred): screenshot size check after full read instead of Stat-first (runtime.go:266)
Task 15: minor (deferred): Engine.Resolve waiters ignore ctx cancellation (install.go waiter branch, bounded ≤660s)
Task 15: minor (deferred): resolveAgentBrowserBinary third candidate reaches ../../frontend/agent-browser source-tree path (daemon.go:559 area; Task 18 packaging should own)
Task 15: minor (deferred): policy_test.go hand-rolls contains/indexOf while importing strings
Task 15: fix round 1/5 dispatched to same implementer (Important #1 state-root containment only; minors parked)
Task 15: fix round 1 DONE — Options.StateRoot threaded (explicit → canonical ~/.operator → fail-closed), config.StateRoot() exported wrapper, daemon passes it with warn-and-disable on failure, fixture tests moved to explicit StateRoot; RED captured then GREEN (adapter/service/daemon/config suites + -race + gofmt clean). Scoped re-review dispatched (sonnet, review-task15-fixscope.diff).
Task 15: fix round 1/5 (1 addressed, 0 open — state-root containment ADDRESSED with file:line evidence; no new Critical/Important breakage)
Task 15: minor (deferred): test sites bind newTestAdapter's 4th return value to stale name dataDir (runtime_test.go:163,~510,735,759) — assertions correct
Task 15: minor (deferred): Options.DataDir dead in production code post-fix (set only by tests)
Task 15: complete pending domain commit (review chain: full opus review SPEC❌/QUALITY❌ on 1 Important → fix round 1 → scoped re-review ALL ADDRESSED — CLEAN; ⚠️ items resolved by controller: TDD ordering evidenced by captured RED compile failures in report; real managed-mode close deferred with fixture evidence noted as system-discovery-mode — carried as informational for Task 20 E2E; Windows compile = standing three-platform CI caveat)
Task 15: complete (commit 5004bfe2a "feat(browser): own standalone automation in daemon", 16 files +3836/−47, base 9271ff7b0)
Task 16: dispatched (base 1ca2add1f55681a763f83991159e33392d369d3d, fresh opus implementer, UNCOMMITTED contract, brief task-16-brief.md)
Task 16: implementer DONE_WITH_CONCERNS (migration 0089 + desktop_preview route + sqlc/openapi/schema regen byte-stable; renderer panel deleted, useExternalPreview created; gate trio 125/125 vitest + Go suites green; RED evidence for six mandated behaviors). Concerns: CDC fan-out per ack, minimal reopen UI, web links external for all session kinds, two inert parity-checker entries (Task 21), interim Electron fallback path via app.openExternal + hook-side ack, pre-existing env failures elsewhere.
Task 16 review SPLIT per gateway-instability precedent into two disjoint half-reviews (opus): goA (backend, review-task16-goA.diff 1546 lines) + rB (renderer/shared/docs/ledger, review-task16-rB.diff 7367 lines); controller merges verdicts.
Task 16 rB (renderer half) verdict: SPEC ❌ / QUALITY NEEDS FIXES — 0C/1I/5M. All deletions/creations/contract changes/behaviors present and tested; ripple files legitimate; Electron preload isolation confirmed. Important: ~55 orphaned i18n browser.* keys ×8 locales unconsumed after panel deletion (constraint requires removal with last consumers). Concerns 2/3/5 adjudicated reasonable by reviewer.
Task 16: minor (deferred, rB): hook pending/error state global vs per-session consumption — failed open on session A self-heals only via remount/revision bump/restart
Task 16: minor (deferred, rB): no direct unit test for tauri-bridge preview path validate→invoke→ack ordering (hook tests mock bridge)
Task 16: minor (deferred, rB): dev-web bridge preview uses window.open, never acks — dev-only re-open per mount
Task 16: minor (deferred, rB): narrow reopen race between successful open and CDC floor delivery can double-open on unmount/remount (server ack idempotent, OS opener not)
Task 16: minor (deferred, rB): dead isInspectorVisible prop threading; SessionView div indent nit; roundabout setError ternary
Task 16 goA (Go half) verdict: SPEC ❌ / QUALITY NEEDS FIXES — 1C/1I/3M. Schema/trigger recreation byte-exact vs 0085 baseline; LAN isolation strongest-mechanism (pre-auth prefix block + double regression pin); envelope preserved; preservation constraints verified.
Task 16 CRITICAL C1 (goA): store/session_store.go:195-200 never sets gen.MarkSessionPreviewOpenedParams.PreviewOpenedRevision_2 — guard binds preview_opened_revision < 0 → zero rows on EVERY ack → all acks 409 PREVIEW_ACK_REJECTED, nothing durable persists, reopen loops forever; fake-based tests at both layers stayed green.
Task 16 Important I1 (goA): no sqlite-backed test covers MarkSessionPreviewOpened :execrows or migration column — gap that let C1 ship green; add integration test vs migrated temp DB (advance/idempotent/stale/future).
Task 16: minor (deferred, goA): concurrent double-ack loser returns 409 instead of idempotent 200 (re-read after applied=false would disambiguate)
Task 16: minor (deferred, goA): comment-convention violations in 0089 sql header, queries/sessions.sql, APIDeps.DesktopPreview field, domain/dto field comments
Task 16: minor (deferred, goA): full-record UpdateSession writers can revert preview_opened_revision on stale read-modify-write (extends pre-existing preview_url/revision hazard class; one spurious reopen worst case)
Merged Task 16 verdict: SPEC ❌ / QUALITY NOT APPROVED → fix round 1 with C1 + goA-I1 + rB-I1 (minors parked).
Task 16: fix round 1/5 dispatched to same implementer (C1 PreviewOpenedRevision_2 + I1 sqlite-backed ack test as its RED + I2 i18n orphan deletion ×8 locales)
Task 16: fix round 1 DONE — RED reproduced C1 against real SQL then GREEN; integration test covers advance/idempotent/stale/future on migrated temp DB; 54–55 keys deleted per locale, line-level only; gates green (storage/service/httpd suites, gate quartet 126/126, typecheck, parity 101, i18n suite 13/13). Scoped re-review dispatched (sonnet, review-task16-fixscope.diff).
Task 16: fix round 1/5 (3 addressed, 0 open — C1 ADDRESSED binding re-verified positionally in generated code; I1 ADDRESSED real-SQL integration test with verbatim-RED provenance; I2 ADDRESSED zero remaining consumers verified independently; no new Critical/Important breakage)
Task 16: complete pending domain commit (review chain: split half-reviews SPEC❌ both halves → fix round 1 → scoped re-review ALL ADDRESSED — CLEAN; ⚠️ items resolved by controller: byte-stable regen evidenced by implementer gates + goA diff inspection of openapi/gen shapes; RED authenticity accepted on verbatim fatal-line match; interim Electron fallback path (concern 5) is Task 21 deletion surface, tracked there; watch item recordToUpdate rollback = parked goA Minor 3)
Task 16: complete (commit 959bde708 "feat(preview): replace embedded panel with external preview", 62 files +1476/−5345, base 1ca2add1f)
Task 17: dispatched (base e5e2969e9ed24f12e54f78b65772e3015d0d0f51, fresh opus implementer, UNCOMMITTED contract, brief task-17-brief.md)
Task 17: implementer killed by API 429 (rate limit) mid-task after RED phase, last narration "Now the engine module"; resuming same agent per Task 10/14 crash-resume precedent (context survives via SendMessage).
Task 17: two more silent mid-task stops resumed via SendMessage (capabilities/bridge stage); third resume completed.
Task 17: implementer DONE_WITH_CONCERNS (updater/{mod,channel,status,escalation,storage,tests}.rs + 8 ACL TOMLs; RED 47 → cargo test --locked 205 passed with 48 new updater tests ×3 green; fmt/clippy/build/typecheck/parity 101/bridge vitest 36/36 green; plugin pin =2.10.1 pre-existing since 782dfa909 so no Cargo diff). Concerns: (1) RECORDED STOP updates_install always errors — claimed plugin install_inner writes to OS-default temp/cache dirs, apply deferred to Task 18, staged builds kept under <state-root>/updater/staged; (2) GitHub transports unwired shell-side, degrade-safe, owner Task 18; (3) feed base URL env-only until Task 18 bakes production default; (4) first-run opt-in implemented+tested but no invocation site (renderer onboarding owns prompt), new users start disabled; (5) boundary proof: check/download in-memory, staging project-owned, TMPDIR stamped to <state-root>/updater/tmp. Reviewer dispatched to verify claim 1 against vendored plugin source and adjudicate all five.
Task 17: opus reviewer dispatched on review-task17.diff (4402 lines incl. untracked contents).
Task 17 review verdict: SPEC ❌ / QUALITY NOT APPROVED — 0C/3I/7M. Concern 1 RECORDED STOP independently VERIFIED TRUE against vendored plugin source (~/.cargo/registry tauri-plugin-updater-2.10.1/src/updater.rs: download streams in-memory; install writes via tempfile::tempdir()/dirs::cache_dir()/env::temp_dir(); builder API offers no destination override). Concerns 2–5 adjudicated accepted by reviewer (degradation semantics match code, owners named).
Task 17 Ruling (recorded apply stop): ACCEPTED as spec-sanctioned "stop that piece" — the pinned plugin cannot honor the state-root boundary on ANY platform for install/recovery writes, so updates_install fails closed with APPLY_DEFERRED_MESSAGE while check/download/staging stay project-owned beneath <state-root>/updater. OWNER: Task 18 must deliver a project-owned verified apply path or explicitly spawn its dedicated follow-up; completion REQUIRED before any release ships (joins notifications:click as release-gating deferral #3). Cost if wrong: shipped builds can never complete an update install if this entry is forgotten — surfaced here and in the finishing review.
Task 17 Important I1: downgrade parity broken — Electron sets autoUpdater.allowDowngrade=true (auto-updater.ts:67,78); PluginFeedClient never sets .version_comparator so plugin default `release.version > current` silently refuses downgrades; return-home to older stable strands user Ok(None); no downgrade test.
Task 17 Important I2: interrupted-download recovery unwired in production — begin_download called only from #[cfg(test)] seam (mod.rs:711); pending_downloads/prune_partials have zero production callers; perform_download calls only complete_download; brief-mandated behavior tested exclusively through dead paths.
Task 17 Important I3: download progress bypasses engine state — mod.rs:440 emits Downloading straight to sink without broadcast*, updates_status goes stale mid-download, escalation guard mod.rs:651 dead in production, escalation tick can rebroadcast stale staged row during download; its test uses mark_downloading_for_test + len>= assertion which cannot fail. Route progress through broadcast_owned and assert equality.
Task 17: minor (deferred): loopback_http hand-rolled HTTP/1.1 corrupts chunked Go responses (fine small-body, fragile)
Task 17: minor (deferred): release_slot retains stale release/feed_url when later check returns NotAvailable; download_now could stage previous-channel artifact
Task 17: minor (deferred): doc comments on private fns mod.rs:247-249/:297-298/:765-766 violate comment rule
Task 17: minor (deferred): open_shell_engine failure logs-and-continues; commands fail at invoke time instead of startup signal
Task 17: minor (deferred): PluginFeedClient::validated_key fail-closed glue untested
Task 17: minor (deferred): published_epoch_ms skips non-Z offsets; duplicated calendar math hand-rolled in test helper
Task 17: minor (deferred): escalation tick reads settings outside op_lock, broadcasts independent=true, mutates automatic_snapshot mid-check
Task 17: fix round 1/5 dispatched to same implementer (I1 downgrade comparator+test, I2 recovery wiring into production paths, I3 progress via broadcast_owned + real-assertion test; recorded stop untouched per ruling)
Task 17: fix round 1 DONE — RED 48p/4f (all three findings red) → GREEN cargo test --locked 209/0 (52 updater tests ×3 green), fmt/clippy/build/typecheck/parity clean. I1 feed_offers_candidate seam + .version_comparator (allowDowngrade parity) + return-home-to-older-stable flow test; I2 perform_download records intent pre-stream + recover_interrupted sweep in open_shell_engine proven via real manual_check→download path; I3 cloneable Broadcast handle routes progress through broadcast_owned, injection test replaced by gated-streaming equality test pinning stale-rebroadcast refusal. Scoped re-review dispatched (sonnet, review-task17-fixscope.diff).
Task 17: fix round 1/5 (3 addressed, 0 open — I1 comparator wiring verified against pinned crate source incl. default-comparator premise; I2 intent-before-bytes + sweep cannot delete staged artifacts, sole production callers proven; I3 no state change bypasses engine status stream, mark_downloading_for_test gone repo-wide; no new Critical/Important breakage)
Task 17: minor (deferred): run_escalation_tick checks Downloading guard before settings.read await — one stale rebroadcast possible if download starts during that await (self-heals next progress callback)
Task 17: complete (commit 746c471b8 "feat(tauri): rebuild updater state and channels", 18 files +4355/−9, base e5e2969e9)
Task 17 Ruling (ownership refinement of accepted concerns): production feed base URL baking AND the project-owned apply-path delivery are Task 18-owned (release-config surface). GitHub HTTPS transports shell-side: Task 18 must either wire them or explicitly spawn the dedicated follow-up in the ledger — decide and rule there. First-run opt-in invocation site: re-owned to Task 20 (parity-closure mandate covers the opt-in row; implement or defer-with-explicit-user-sign-off there). Cost if wrong: an orphaned parity gap ships silently — mitigated by both tasks' dispatches carrying these mandates.
Task 18: dispatched (base d60397a0f7c6787935cd47b7d853d2268a15f15f, fresh opus implementer, UNCOMMITTED contract, brief task-18-brief.md)
Task 18: first dispatch returned EMPTY (no status, zero work landed, no report file — Task 9 failure mode); re-dispatched fresh opus implementer with identical contract at unchanged base.
Task 18: second dispatch DONE_WITH_CONCERNS (tauri-feed.mjs+21 tests, package-tauri-mac-zip.sh, verify-tauri-artifacts.sh, feed/e2e-mac-update ports, 7 workflow files incl. desktop-testing.yml DELETION, tauri.release.conf.json, spawn_updater_timers added to Task-17 engine; RED→GREEN 44 node:test + all suites green + cargo 213/0 ×3 + typecheck/parity clean; local unsigned E2E exercised: build → ditto zip → ephemeral-key signed updater archive crypto-verified → feeds generated → verifier structural passes). Concerns: launch-timer production change composition-tested only; pubkey empty in release conf injected via OPERATOR_UPDATER_PUBLIC_KEY --config overlay; disabled-at-HEAD desktop-testing.yml deleted not ported; rulings 2+3 SPAWNED as follow-up briefs (followup-verified-apply-brief.md, followup-github-transports-brief.md) instead of delivered — both release-gating; nightly schedule inherited-absent.
Task 18 review SPLIT into two disjoint half-reviews (opus): scriptsA (scripts/docs/package.json) + wfB (workflows/src-tauri/follow-up briefs); controller merges verdicts. First scriptsA reviewer died empty ("No assistant messages found") and was re-dispatched fresh.
Task 18 scriptsA (scripts half) verdict: SPEC ✅ / QUALITY NEEDS FIXES — 0C/2I/8M. All nine rejection cases behavior-real; determinism pinned byte-for-byte incl. input-order reversal; mac permanence invariant enforced at build AND verify time; AGENTS.md seal rule intact; ports lose no coverage; honest gate ledger.
Task 18 Important SA1: private-key scan has realistic naming bypass — assertNoPrivateKeyMaterial reads only PRIVATE_KEY_NAME-matching files; a minisign secret key named t18key/key/signing_key (exactly what `tauri signer generate -w` produces, and the implementer's own exercise key name) is never content-scanned and publishes verbatim with the dist. Fix: content-scan first bytes of every file for minisign secret-key header.
Task 18 Important SA2: licenses never inspected — check_bundle_resources omits licenses from every package form's check list and no GATE records it (spec mandates licenses in every base artifact + resource inspection).
Task 18: minor (deferred, scriptsA): duplicated unreachable secret-key throws in validateSignature + dead base64 try/catch wrappers; self-review item 5 inaccuracy
Task 18: minor (deferred, scriptsA): unused writeBlockmap import in feed.test.mjs
Task 18: minor (deferred, scriptsA): bracketed IPv6 [::1] rejected by both validators despite listing loopback (fail-closed direction, dev-only)
Task 18: minor (deferred, scriptsA): e2e relaunch fixed sleep(5000) vs bounded-poll principle
Task 18: minor (deferred, scriptsA): verifier nits — unquoted $pattern glob, substring version check, unescaped JSON embeds, --release-date unvalidated
Task 18: minor (deferred, scriptsA): dead .exe&&!endsWith(.sig) clause
Task 18: minor (deferred, scriptsA): coverage nits — no end-to-end nightly compat-YAML test; feature-channel test misses pr*.yml trio; wrong-arch assets skipped not refused
Task 18: minor (deferred, scriptsA): pervasive explanatory comments mirror existing frontend/scripts register — controller to confirm house-style exemption for scripts dir (pre-existing convention)
Task 18: session restart mid-review-loop — prior controller died after recording scriptsA verdict but before wfB verdict landed; working tree verified byte-identical to reviewed state (HEAD d60397a0f, all Task 18 work still UNCOMMITTED per contract). wfB half-review RE-DISPATCHED fresh (read-only opus, same scope: 7 workflows + updater/mod.rs+tests.rs + tauri.release.conf.json + both follow-up briefs; same package review-task18-wfB.diff). Fix loop not yet started — will run once both half-verdicts merge.
Task 18 Ruling (deferred minor, scriptsA comments): house-style exemption CONFIRMED for frontend/scripts — dense explanatory comments are that directory's established register (feed.mjs at HEAD d60397a0f: 66 of 140 lines carry //; verify-mac-artifact.sh: 58 # lines pre-existing). Not a defect; no change ordered. Cost if wrong: cosmetic comment density only.
Task 18 wfB (workflows/src-tauri/briefs half) verdict: SPEC ❌ / QUALITY NEEDS FIXES — 0C/5I/10M. Strengths: channel isolation strengthened (latest*/nightly*.json rejection), release-latest-guard structural validation, resolve_feed_base_url total+fail-closed with real tests, secrets hygiene clean, launch-tick single-caller (lib.rs:1091) Electron parity, desktop-testing deletion documented, zero stale forge references.
Task 18 Important WB1: strict-trust gate advertised but never armed — verify-tauri-artifacts.sh runs WITHOUT --strict-trust in feature-release.yml (2 sites) + frontend-release.yml (2 sites incl. intel leg) despite step names "strict trust on this runner"; trust checks recorded but cannot fail signed releases. Fix: add --strict-trust to the four signed-CI invocations; keep --mode testing runs as-is.
Task 18 Important WB2: mac-update-e2e.yml baked-feed assertion can never pass — greps installed binary for contiguous "releases/latest/download/latest.json" but join_feed_file concatenates base+manifest at runtime so the joined string never exists in rodata; monitor permanently red. Fix: grep baked base URL alone.
Task 18 Important WB3: e2e-gate blocking enforcement silently deleted — frontend-release.yml lost the OPERATOR_GATE_BLOCKING step while report claimed pod job "preserved verbatim". Ruling: RESTORE the blocking-enforcement step (faithful port; retiring an existing control is a user decision, not mine).
Task 18 Important WB4: build-artifacts digests.json regression — digest loop walks only dist-artifact/operator-*; deb/rpm live in dist-deb/dist-rpm and dmg in dist-macos-extra; deleted steps had merged deb/rpm hashes. Conductor hash-before-signing now covers 3/6 published artifacts. Fix: digest every collected class.
Task 18 Important WB5: rustup toolchain pin ineffective in six jobs — `rustup toolchain install 1.96.0` never selects (no rust-toolchain.toml anywhere, no RUSTUP_TOOLCHAIN env); cargo builds under runner-image stable = non-reproducible artifacts. Fix: rustup default 1.96.0 or job-level RUSTUP_TOOLCHAIN alongside each install.
Task 18 wfB minors (deferred): #6 misleading unsigned-fallback header in frontend-release.yml; #7 release-latest-guard missing x64 zip/dmg aliases; #8 AppImage .sig copied with || true at collection; #9 feature tag targets GITHUB_SHA not headSha on workflow_dispatch; #10 testing-build Linux inspect ::error under continue-on-error contradiction; #11 ${{ inputs.version }} direct interpolation in build-artifacts run block; #12 verified-apply brief lacks named owner + migration-scenario sentence; #13 github-transports brief states no release-gating status; #14 file-pin test under-specifies URL (owner/repo typo passes Rust-side); #15 no isolated launch-time tick test.
Task 18 ⚠️ resolutions by controller: (a) build-artifacts upper matrix legs unchanged context — inherited composition, no gap; (b) repo secret existence operational/outside diff — noted for release conductor; (c) nightly schedule workflow absent — matches brief's seven-file contract AND pre-existing state at HEAD (old doc referenced frontend-nightly.yml which never existed here). Ruling: inherited-absent accepted for Task 18; nightly tooling fully supported; schedule workflow remains open work outside this task's file contract. Cost if wrong: nightly channel has no producer until a schedule workflow lands.
Task 18 implementer-concern adjudications (wfB evidence): concern 1 launch-time tick ACCEPTED (single caller, parity, coalesces behind in-flight guards; isolated-test suggestion parked as minor #15); concern 2 pubkey-via-overlay ACCEPTED (secrets hygiene verified clean); concern 3 desktop-testing deletion UPHELD (disabled at HEAD, documented in survivor header); concern 4 nightly → ruling above; concerns 5+6 external-gate posture ACCEPTED.
Task 18 Ruling (minor #13 substance): github-transports follow-up is NOT itself release-gating — Task 17's accepted design ships with degrade-safe unwired transports (Electron-unreachable-GitHub parity) until wired; the brief's interim posture matches standing rulings and only failed to restate them. Verified-apply follow-up REMAINS release-gating per Task 17 ruling (deferral #3); its owner is the dedicated follow-up dispatch that must complete before any release ships; migration-scenario coverage rides on mac-update-e2e full-install mode which is already authored to assert once apply lands.
Task 18: fix round 1/5 dispatched — fresh opus implementer (prior implementer's session died; report file is persistent memory), findings SA1 SA2 WB1 WB2 WB3 WB4 WB5 verbatim, all work stays UNCOMMITTED.
Task 18: fix round 1 implementer DONE (fresh opus, 71 tool calls): SA1 content-scan via shared PRIVATE_KEY_MATERIAL + strict RS-packet structure + bounded 64KiB prefix scan of EVERY regular file (RED 21→24 tests, GREEN 47/47); SA2 licenses required in all four mac package forms + NSIS 7z listing w/ named gate fallback + deb/rpm listing checks (synthetic-dist positive 26/0/4gated exit0, negative exit1); WB1 --strict-trust at exactly the four signed-CI sites; WB2 grep baked base URL alone; WB3 enforce step restored byte-identical from d60397a0f; WB4 digests walk dist-deb/dist-rpm/dist-macos-extra + restored Upload digest step (disclosed as required for merge job inputs); WB5 job-level RUSTUP_TOOLCHAIN=1.96.0 at all six sites. bash -n clean; js-yaml parses five touched workflows OK. Nothing staged. Scoped re-review dispatched (sonnet, package review-task18-fix1.diff).
Task 18: fix round 1 re-review verdict: ALL SEVEN ADDRESSED (SA1 content-scan w/ RS-packet structure pinned by 3 new tests; SA2 licenses all forms; WB1 four sites exact; WB2 base-URL-alone grep; WB3 byte-identical context-line restoration proof; WB4 upload-step restoration ADJUDGED WITHIN INTENT; WB5 six-site env pin). BUT round introduced Important NB1 + surfaced out-of-scope intel defect → round 2.
Task 18 Important NB1 (new breakage, in-loop): --strict-trust × gate taxonomy bricks all four signed legs deterministically — sibling-arch absence gates (arm64 leg lacks x64 archive verify-tauri-artifacts.sh:330-332), conductor-side Authenticode/smoke/rpm-K gates recorded even where host holds tooling (:394-395,:438), terminal rule exits 1 on ANY gate under strict trust (:484-487). A perfectly executed signed release cannot go green.
Task 18 Important INTEL (out-of-scope observation PROMOTED to loop by controller ruling): release-mode verifier demands an arm64 updater archive in EVERY darwin dist (:317-325) but the macos-15-intel leg's dist holds only x64 → frontend-release.yml:309 + feature-release.yml:480 fail structurally regardless of NB1. Pre-existing Task 18 script logic missed by scriptsA review; promoted because parking it would ship a bricked release leg — same load-bearing class as NB1.
Task 18 Ruling (NB1+INTEL resolution semantics): verifier gate model becomes explicit three tiers. (1) FAIL — structural violation or trust check attempted-and-failed (unchanged). (2) GATE — unverifiable WITHOUT CONDUCTOR-HELD MATERIAL even on a fully-equipped CI host (Authenticode validation evidence, rpm -K against conductor-imported key, NSIS silent-smoke, notarization status pre-conductor): recorded by name, NEVER fatal under --strict-trust; in-workflow trust OPERATIONS (signtool signing, notarytool) already fail their own steps naturally on failure, so nothing is lost. (3) NEW SCOPE-SKIP via explicit invocation scoping (--arch complementing existing --platform): artifact-class absence caused by matrix topology (leg never produces sibling arch/platform artifacts) is neither PASS nor GATE nor FAIL. --strict-trust then means: every check applicable to declared scope executable on this host ran AND passed — preserving WB1's requirement that trust can fail a release (mac codesign/spctl/staple trio via delegation stays strict-fatal) while a correctly-executed signed leg goes green. Cost if wrong: over-permissive gates could mask a bad signed mac artifact — mitigated: mac trio remains fatal under strict trust, and it is the only trust verification the plan text mandates.
Task 18: minor (deferred, round-1 residual): assertNoPrivateKeyMaterial scans dist TOP LEVEL only (non-recursive); nested-subdir key escapes scan — narrowed defense-in-depth breadth, SA1 vector itself closed (feed inputs also top-level only). Report bookkeeping nit (inconsistent negative-run counts between halves) immaterial, noted not ledgered as finding.
Task 18: fix round 2/5 dispatched — RESUMED round-1 fixer (context intact) with NB1 + INTEL + ruling semantics.
Task 18: fix round 2 implementer DONE: three-tier ledger model landed (FAIL / conductor-material GATE / SCOPE-SKIP INFO), --arch scoping w/ per-arch darwin loops, blanket strict-gate exit removed while mac seal trio stays strict-fatal; matrix legs scope --arch arm64|x64 keeping --strict-trust; docs rewritten. Evidence: scoped strict run exit0 zero-x64-rows; unsigned fixture under strict exits1 via failed trio; unscoped default byte-compatible legacy; x64-only dist fails unscoped (INTEL reproduced) passes scoped; bash -n clean; workflows js-yaml OK; node trio 47/47. Concerns: NSIS license check softened to tier-2 gate (7z NSIS listing fidelity unprovable locally — narrows SA2 win32 coverage slightly, deb/rpm stay hard FAIL); scenario-1 positive proven via codesign-shadowed PATH (no Apple identity locally) with attempted-and-failed fatality separately proven.
Task 18: fix round 2 re-review verdict: NB1 ADDRESSED, INTEL ADDRESSED, no new Critical/Important breakage — three tiers verified in code (tier defs :9-29/:37-42, scope_skip INFO isolated from ledger arrays + emit-gates JSON, per-arch loops w/ legacy unscoped branches preserved verbatim, blanket strict rule gone exit1 only on FAILURES :639-642, mac trio sole strict-fatal trust check :383-393, conductor-material gates never fatal). Adjudication 1: NSIS-license softening ACCEPTED (round-1 hard-fail was fragile-parse negative = NB1 disease; SA2 cured on every reliably inspectable surface). Adjudication 2: shadowed-PATH positive + unsigned-fixture negative ACCEPTED as sufficient local proof; genuinely-signed trio success declared EXTERNAL GATE honestly.
Task 18 minors (deferred, round-2 delta cosmetics): gate() streaming prefix changed ("conductor-side material needed, recorded") so live console echo differs though ledger rows/counts/exit codes identical; scoped dmg failure message says *$arch* though aarch64/x86_64 globs also satisfy.
Task 18 Important BUILD (out-of-scope observation PROMOTED to loop by controller ruling, verified independently against working tree): build-artifacts.yml verify step bricks ALL FOUR legs structurally — (a) --expect-version drives filename grep (verify-tauri-artifacts.sh:302-307) but build-artifacts collects VERSION-FREE aliases by contract (operator-darwin-arm64.zip :121, operator-win32-x64.exe :125, operator-linux-x64.AppImage :128) so "no artifact name mentions $VERSION" fails every run; (b) unscoped darwin legs demand *.dmg in --dist dist-artifact (:452-455) but dmgs collect into dist-macos-extra (:122) → "no .dmg in $DIST". Same bricked-CI-leg class as INTEL; promotion consistent with INTEL precedent.
Task 18 Ruling (BUILD resolution): verifier gains repeatable --extra-dist <dir> consulted by class-absence checks (dmg/deb/rpm) before failing — found-in-extra artifacts get FULL inspection where they live; plus an explicit opt-in flag declaring version-free-alias layout under which the :302-307 filename grep passes-on-note instead of failing (embedded-metadata version assertions like the Info.plist equality check remain fully active). build-artifacts.yml passes the alias flag + the sibling class dirs as --extra-dist. Rationale: conductor pipeline keeps deep structural inspection (incl. dmg hdiutil) without duplicating uploads; release pipelines with versioned dists are unaffected. Cost if wrong: a conductor dist could pass with wrong embedded versions — mitigated: plist/exe metadata equality checks stay active whenever forms provide them.
Task 18 minors (deferred, re-review out-of-scope): artifact-gates.json emitted on signed legs but no upload-artifact step persists it (gate ledger lives in step logs only); deb/rpm license detection greps listing text for LICENSE while Debian convention ships copyright (implementer concern 6; tighten on native-runner evidence).
Task 18: fix round 3/5 dispatched — RESUMED same fixer (context intact) with BUILD + ruling.
Task 18: fix round 3 implementer DONE: repeatable --extra-dist consulted by every class-absence check (sibling classes inspected in place w/ provenance), --expect-aliases opt-in (filename grep → named PASS note; plist assertions active), find_across_dists/require_file_in helpers; build-artifacts.yml passes flag + three extras + arch convention, mode testing + emit-gates unchanged. Evidence: BUILD-topology positive exit0 w/ dmg-inspected-from-extra provenance rows; license-less variant exit1 on both forms; release path unaffected (versioned dist passes unflagged; alias-only unflagged reproduces defect verbatim); linux-leg topology finds deb/rpm from extras; bash -n clean; js-yaml exact-command check; node trio 47/47. Mid-round self-caught bug: phantom empty element in extra-dirs loop exited 2 on every unscoped run — fixed before finish, disclosed.
Task 18: fix round 3 re-review verdict: BUILD ADDRESSED, no new Critical/Important breakage — --expect-aliases opt-in w/ filename grep else-branched (:706-713) while plist equality stays active (:681-689); repeatable --extra-dist via find_across_dists (:594-620) routed through ALL class-absence sites (updater :830, unscoped zip/dmg :851/:859, scoped :882/:890, NSIS :907, AppImage/deb/rpm :956/:969/:977) with in-place full inspection + provenance; build-artifacts wiring exact (:139-159); release invocations verified untouched by grep; phantom-element regression CONFIRMED FIXED (guard :491 + set-u-safe find_across_dists :608-609); evidence adjudicated SUFFICIENT (17-pass arithmetic coheres, extras negatives bite, genuine opt-in semantics proven by the pass/reproduce control pair).
Task 18: minor (deferred, round-3 delta): require_file() now dead (zero callers after require_file_in refactor); absence-FAIL message texts gained "or any --extra-dist" suffix (cosmetic drift).
Task 18: minor (deferred, re-review out-of-scope): under --expect-aliases win32/linux legs have no embedded-metadata version backstop (no PE/AppImage assertion exists) — enforcement advisory there, darwin enforces fully; tighten if conductor-side win/linux version assurance matters. (inputs.version interpolation note duplicates already-deferred wfB minor #11.)
Task 18: complete pending commit — fix loop closed after 3 rounds: round1 SA1 SA2 WB1-WB5 all addressed; round2 NB1+INTEL addressed; round3 BUILD addressed. Deferred minors parked above for final-review triage.
Task 18: complete (commits d60397a0f..84981fe40, review clean after 3 fix rounds — round1: SA1 SA2 WB1-WB5; round2: NB1 INTEL; round3: BUILD; 0 open findings). Deferred minors parked above (scriptsA x8 incl. resolved-by-ruling comments exemption, wfB #6-#15, round-2 cosmetics x2, round-3 dead-helper + message drift, alias-mode win/linux advisory-version note, gates-json-not-uploaded, deb/rpm copyright-convention) for final whole-branch review triage.
Task 19: dispatched (base 84aec025f, fresh opus implementer, UNCOMMITTED contract override of brief Step 4 git block, brief task-19-brief.md). Interfaces verified present: bench:shell, tauri:build, test:e2e:renderer (@T0|@P0 grep), routes tree as planned.
Task 19: implementer run interrupted by controller session restart (no completion record); partial work verified in tree — 4 new script files + perf/terminal harness modifications + perf/results/ before-data, NO renderer changes yet, no report. SAME agent resumed with tree-state briefing + added requirement: justify-or-revert each out-of-brief perf/terminal hunk in an explicit report section.
Task 19: resumed implementer DONE_WITH_CONCERNS (221 tool calls): route-bundle-report + heap-summary scripts/tests, checked-in before/after evidence JSONs, TerminalPane disposal changes (+new test), telemetry.ts PostHog deferral, perf/terminal harness instrumentation (justification section in report), baseline doc updated. RED→GREEN 17/17 node:test; vitest 1530/1530; harness 25/25; Playwright @T0/@P0 21/21; typecheck clean; parity 101. Headline: board-paint parse set −34.5% (3126370→2047720 bytes; xterm 593KB + PostHog 519KB behind dynamic boundaries), zero forbidden eager edges, flat RSS across 8 mount/write/dispose cycles. Concerns: binding warm-start/idle-memory gates open pending native runners (benchmark-shell electron-only refusal recorded verbatim); empty-board RSS flat within noise (proven win is startup parse bytes); two pre-existing local failures documented. Task reviewer dispatched (opus, package review-task19.diff) with controller context: uncommitted contract, perf/terminal justification requirement, untouched-listed-files-vs-measurement-mandate tension to rule on evidence.
Task 19 review verdict: SPEC ✅ (one ⚠️) / QUALITY APPROVED — 0C/1I/7M. Reviewer independently recomputed parsedBytes sums (3126370→2047720 = −34.50% exact), disposal arithmetic (all 16 deltas negative both runs), verified xterm referenced ONLY by TerminalPane+its tests so the lazy boundary is real not cosmetic, confirmed telemetry consumers touch no posthog default, confirmed benchmark-shell.mjs:30 electron-only refusal verbatim, ruled untouched routes/+XtermTerminal.tsx CORRECT restraint under the measurement-driven mandate (routes pre-split per before.json ?tsr-split ids; XtermTerminal disposal acks pre-existed).
Task 19 Ruling (⚠️ resolution, Important #1): the brief's "before/after shell results" interface item is a REAL GAP uncloseable on this host — WKWebView exposes no observation channel, benchmark-shell.mjs refuses --shell tauri (pre-existing Task 2 guard, unchanged), packaged Electron cannot launch locally. Ruled a TRACKED EXTERNAL GATE attached to Task 19 completion (native-runner warm-start/idle-memory legs must produce checked-in results before the Phase 0 performance decision changes); faking numbers forbidden. NO fix loop — no local fix exists. Joins the release/perf external-gate ledger alongside Task 17 apply-path and notifications:click deferrals. Cost if wrong: perf gates silently never measured — surfaced here, in baseline doc's non-binding-probes section, and in finishing review.
Task 19: minor (deferred): route-bundle-report xterm exemption over-broad (exempts ALL categories off one dynamic edge, route-bundle-report.mjs:78); CSS accumulation double-count risk in totals Map (:122, latent only); memoized rejection permanently disables telemetry/terminal for session (telemetry.ts:16, TerminalPane.tsx:46 React.lazy cache); heap-summary prefix-based traversal guard (:346) admits sibling dirs (loopback probe-only; path.relative correct form); webviewRuntimeVersion synthetic string; emitReport argv laxity vs parseHeapArguments strictness; --start-ms below 1000ms baseline could sample mid-cycle.
Task 19: minor (deferred, controller infra note): review package omitted TerminalPane.test.tsx (controller packaging omission — file verified on disk by reviewer; noted so final review knows the task-19 diff artifact is incomplete there).
Task 19: complete (commits 84aec025f..41dacb80c, review approved; 1 external gate tracked — native-runner warm-start/idle-memory shell results).
Task 20: dispatched (base 3c9e497c1, fresh opus implementer, UNCOMMITTED contract override of brief Step 4 git block, brief task-20-brief.md). Carried mandates in dispatch: Task 17 re-owned first-run opt-in invocation site (implement or defer-with-explicit-user-sign-off, ruling at ledger 358); exact-pin constraint for @wdio/* + both Rust WDIO plugins; e2e-feature isolation requirement.
Task 19: docs commit 3c9e497c1 (ledger completion).
Task 20: implementer (opus, resumed after controller-session restart mid-run) died on API 429 rate limit AFTER reporting "Everything is verified" and BEFORE writing the report — tree verified to contain the full implementation uncommitted: e2e-tauri/ + tsconfig.e2e-tauri.json + tauri-webdriver.yml, Cargo feature gating (Cargo.toml/Cargo.lock/lib.rs), exact npm pins (package.json/package-lock), parity-ledger update, build-contract script+test, e2e-tauri-run.mjs runner, UpdateOptInPrompt.tsx+test+8-locale i18n (opt-in mandate IMPLEMENTED not deferred), tauri-bridge changes+test, main.tsx/_shell.tsx wiring. Agent RESUMED with a report-only instruction (no source mutations allowed); review dispatches once the report lands.
Task 20: resumed implementer DONE_WITH_CONCERNS (report-only finish, no source mutations): 11/11 WDIO E2E x5 runs incl. 2 consecutive clean; build contract 9/9 unit + LIVE 4-leg binary proof (plain 0 driver markers vs e2e-binary 1); cargo 213/0 both feature sets; clippy/fmt/typecheck+e2e-tsconfig/parity-101/vitest-43/Playwright-21 all green. E2E gate caught and fixed TWO REAL PARITY DEFECTS in tauri-bridge.ts (notification_show, tray_attention_state wrong payload shapes). Opt-in mandate IMPLEMENTED (UpdateOptInPrompt + boot marker main.tsx/_shell.tsx + 5 keys x8 locales). Concerns: Win/Linux CI legs authored-unexecuted; environmental flake self-heals; early seams-test version leaked a Ctrl+E global override that spawned a real orchestrator session (killed; test now applies/reverts immediately — flagged for reviewer scrutiny); ledger lines deferred to controller at commit time. Reviewer dispatched (opus, package review-task20.diff, lockfiles summarized w/ numstat + pin excerpts).
Task 20 review verdict: SPEC ⚠️ mostly-compliant w/ gaps / QUALITY NEEDS FIXES — 0C/2I/10M. Verified: three-layer feature isolation (cargo-tree positive control + cfg scan + byte-marker proof on real binaries), bridge fixes REAL against native.rs signatures (:211-214, :332-337), opt-in mandate correctly implemented w/ disabled-by-default intact end-to-end, CI hygiene clean, hotkey incident residual bounded (adjacency-based cleanup noted minor).
Task 20 Important T20-1: brief Step-1 item "run automatic and manual external preview" ABSENT from desktop.e2e.ts AND undisclosed in report's enumeration/weak-points — silent drop of an enumerated scenario. Fix: add drivable assertion (happy-path open_external IPC ack or validator-matrix probe) OR record explicit deferral/sign-off in ledger+report.
Task 20 Important T20-2: parity-ledger main/relocation.ts entry claims prover "contract:rust-relocation+external:tauri-webdriver-macos" but (a) that gate is THIS task's own CI leg (contradicts external: vocabulary) and (b) nothing anywhere asserts relocation behavior. Fix: name a real prover (actual relocation contract test / phase-0 state audit) or leave open honestly.
Task 20 minors (deferred): #3 opt-in Accept consumes once-only ask when settings write fails (setItem precedes set; rejected set → permanently un-asked + still disabled behind console.warn); #4 seams-test hotkey revert adjacency-based not finally-based; #5 benchmark JSON/app-under-test failure artifacts absent from workflow upload list (partly vacuous); #6 OPERATOR_E2E_RENDERER_URL dead knob half-wired; #7 registrationErrors guard-braces imprecision; #8 build-contract main-module detection breaks on spaces vs robust sibling idiom; #9 test-11 reload race latent flake; #10 deepEqual pins entire updates object; #11 ledger pretty-print ~9x line growth; #12 build-contract uses debug builds though brief says production (sound inference, unstated delta).
Task 20 ⚠️ resolutions by controller: Win/Linux legs authored-unexecuted ACCEPTED as standing external-gate posture (first native run = proof obligation, joins gate ledger); additive-only package-lock claim ACCEPTED on numstat+excerpt evidence (final review may re-verify).
Task 20: fix round 1/5 dispatched — RESUMED same implementer with T20-1 + T20-2.
Task 20: fix round 1 implementer DONE_WITH_CONCERNS: preview coverage ADDED (manual path validator matrix via real open_external IPC — ftp/file/javascript rejected, http asserted as validator-acceptance honestly scoped cross-platform; automatic path proven via app-owned daemon route acks REVISION_REQUIRED/INVALID_JSON/SESSION_NOT_FOUND explicitly not ROUTE_NOT_FOUND; restRaw helper; ftp case relocated out of seams test) → WDIO 12/12 x2 consecutive; relocation entry repointed to contract:rust-relocation+open:real-bundle-relocation-move citing app_state_tests.rs (17 named tests) with real signed-bundle ditto move + relaunch remainder marked OPEN. Parity checker 101 + ledger tests 15/15; typechecks clean. Scoped re-review dispatched (sonnet, review-task20-fix1.diff).
Task 20: fix round 1 re-review verdict: T20-1 ADDRESSED (dual-layer honest coverage verified through useExternalPreview composition funnel; scoping ruled sufficient), T20-2 ADDRESSED (app_state_tests.rs prover REAL — every enumerated test name exists verbatim incl. decision matrix/failure paths/cross-process lock; open remainder precisely worded; external: vocabulary restored). Silent-edit check CLEAN across all 101 statuses. New breakage: none C/I. Task 20: minor (deferred): fix-report prose miscount 17-vs-15 relocation test figures (corrected by controller in task-20-report.md at close; code unaffected); check-parity-ledger.mjs never constrains status vocabulary (pre-existing; status honesty rests on report definitions).
Task 17 Ruling (first-run opt-in invocation site, re-owned to Task 20): DISCHARGED — implemented as UpdateOptInPrompt (mount gated on daemon-ready + native shell + not-answered + enabled===false; Accept-only writes enabled:true; asked-flag persisted with storage-failure abort so the ask retries), boot marker in main.tsx, _shell.tsx mount, 5 i18n keys x8 locales; disabled-by-default verified end-to-end by vitest 7 tests + daemon-backed E2E test. No sign-off deferral needed.
Task 20: complete (commits 3c9e497c1..19662f27b, review clean after 1 fix round — T20-1 preview coverage added honestly, T20-2 relocation entry repointed to real prover; 10 deferred minors parked). External gates: Win/Linux tauri-webdriver legs authored-unexecuted until first native run; release-gating externals unchanged (verified-apply #3, notifications:click #1).
Task 21: dispatched (base 1cd8a90ed, fresh opus implementer, UNCOMMITTED contract override of brief Step 4 git block, brief task-21-brief.md). Deletions restricted to the brief's explicit file/dir list only; working-tree deletions, nothing staged ever.
Task 21: implementer DONE_WITH_CONCERNS (253 tool calls): 79 tracked paths deleted (main.ts/preload*/annotate-preload*/src-main 23 modules/8 shared modules+tests/forge.config/vite.main+preload configs/makers//browserruntime/), no-electron.test.mjs 15 assertions RED 13/15→GREEN, parity checker EVOLVED (preload.*→bridge.* 55 rows, +1 bridge.preview row, 46 archived verbatim + reappearance rejection, 18/18 tests, ledger 102 entries), VITE_RENDERER_PREVIEW rename across renderer/e2e/configs w/ fixtures unchanged, daemon.go broker unwired → standalone adapter wired into session teardown, 11 Electron packages uninstalled (lock: 0 added / 394 removed; arborist crashes + --force retries disclosed), tauri-phase0.yml trigger-disabled not retired (legacy jobs call deleted npm run package), docs/CLAUDE.md/RUN_APP_COMMANDS stale-reference updates. Gates: typecheck x2 exit0, Playwright 21, WDIO mac leg 12, tauri:build exit0 app+dmg, go test green modulo 2 proven-pre-existing flakes. Dead-but-outside-list flagged for controller/T22: feed.mjs→blockmap.mjs→app-builder-lib chain, ensure-browser-runtime.mjs, feature-channel-resolution.test.mjs, electron benchmark legs + phase0 legacy scripts, browser-view-types.ts. Reviewer dispatched (opus, review-task21.diff deletion-list packaging) with phase0-posture judgment + checker-hollowing check + teardown-wiring verification assignments.
Task 21 review verdict: SPEC COMPLIANT / QUALITY APPROVED — 0C/2I/5M. Verified: deletion set exactly the brief list path-for-path; broker precondition REAL (startSession takes BrowserLifecycle fed by standaloneBrowser daemon.go:255; destroyBrowserBestEffort manager.go:1433-1443 w/ named tests manager_test.go:2122/:5010 + adapter runtime_test.go:483/525/545); lockfile PROVEN pure removals by JSON-map diff (0 added / 394 removed / 0 survivor version changes); rename zero-residue; parity checker EVOLVED NOT HOLLOWED (live TS-AST inventory of createTauriBridge literal, archive class reappearance-rejecting — new guarantee the Electron-era checker never had); absence test behavior-real end-to-end incl. enforced phase0 trigger-freedom invariant.
Task 21 Ruling (phase-0 posture): trigger-disabling tauri-phase0.yml ACCEPTED as honest stop — legacy jobs invoke deleted npm run package and would be guaranteed-red; rewriting kill-gate machinery to keep them green would be the dishonest move; decision record lives in docs/benchmarks/tauri-port-baseline.md + commits; disabled state ENFORCED by absence-test invariant; zombie-body retirement correctly routed to Task 22 structural cleanup. Cost if wrong: none identified — gate record intact elsewhere.
Task 21 Important T21-1: report's dead-candidate handoff MISCHARACTERIZES feed.mjs+blockmap.mjs+app-builder-lib as "uncalled by any workflow or script" — FALSE: tauri-feed.mjs:18 imports generateFeeds (called :377) for the compat YAML feeds uploaded by every production release; blockmap.mjs module-level requires app-builder-lib even with {blockmap:false}. Keep decision RIGHT; flag text must be corrected BEFORE Task 22 intake else T22 acting on it breaks the release pipeline.
Task 21 Important T21-2: AGENTS.md contradicts the deletion — :99 retains the main.ts userData-override paragraph (twin of the CLAUDE.md paragraph this task rewrote shell-neutral), :8 still "Electron + React supervisor"; undisclosed omission from stale-doc sweep; docs/README.md:4 + docs/architecture.md browser-capture sections share it at lower stakes.
Task 21 minors (deferred): #3 app://renderer survives in live backend CORS (cors.go:79 seeded config.go:79) unflagged; absence-test app://renderer sweep never covers backend/scripts; #4 bridge.preview/openExternalPreview row omits status field (checker doesn't validate status — drift passes silently); #5 archive class permits inventing rows for never-existent surfaces; phase0 workflow body can rot unnoticed (all stale tokens skipped); #6 absence-test brittleness (browser-word ban prose fragility; dir-skip list leaves backend/internal/daemon unswept; import scan covers src only not e2e/scripts); #7 vestigial OPERATOR_AGENT_BROWSER_TEST_BINARY in frontend.yml:51.
Task 21: fix round 1/5 dispatched — RESUMED same implementer with T21-1 (correct handoff text) + T21-2 (stale-doc cleanup: AGENTS.md, docs/README.md, docs/architecture.md).
Task 21: fix round 1 implementer DONE: report item rewritten as "Release-mandatory chain — DO NOT REMOVE" w/ exact call-site citations (tauri-feed.mjs:18/:377, feed.mjs:13→blockmap.mjs:10, frontend-release.yml:450 / feature-release.yml:550); AGENTS.md :8 Tauri+React + shell-neutral hard-rule w/ lib.rs webview pin cited (:71 electron-updater rationale deliberately retained as compat-YAML justification); docs/README.md :4 updated; architecture.md Browser Runtime Bridge → Standalone Browser Runtime section rewritten from daemon-owned agentbrowser facts. Gates re-run post-docs: no-electron 15/15, checker 18/18 + ledger 102, typecheck 0. Scoped re-review dispatched (sonnet, review-task21-fix1.diff).
Task 21: fix round 1 re-review verdict: T21-1 ADDRESSED (DO-NOT-REMOVE section w/ every citation verified verbatim incl. blockmap.mjs:10 top-level require semantics + workflow invocations :450/:550; mischaracterization retracted by name), T21-2 ADDRESSED (all four surfaces shell-neutral + factually verified vs code incl. policy.go:33-34 blocked-flag list, runtime.go:621-629 stable codes, TestExecuteRejectsDesktopPanelOnlyActions runtime_test.go:428; no hard rule weakened; :71 retention ruled coherent consumer-side rationale reinforcing the DO-NOT-REMOVE marker).
Task 21 minors (deferred, round-1 re-review out-of-scope): architecture.md:56 mermaid node still FE[Electron + React UI]; AGENTS.md:44 retains desktop-browser-panel sentence (stale since Task 16); lib.rs citation lacks frontend/ prefix (trivial).
Task 21: complete pending commit — review approved, both Importants fixed and re-reviewed, 0 open findings.
Task 21: complete (commits 1cd8a90ed..7a90aae9d [amended once to include backend/internal/daemon/daemon.go which escaped the controller's initial staging scope — no intermediate SHA was ever referenced], review approved after 1 fix round; 2 Importants fixed+re-reviewed; 12 deferred minors parked). Electron is gone; parity checker guards the live Tauri bridge (102 entries) with archive reappearance-rejection.
Task 22: dispatched (base 067aeff5c, fresh opus implementer, UNCOMMITTED contract override of brief Step 3 git block, brief task-22-brief.md). Carried dispatch context: Step-2 matrix honesty rule (locally-runnable subset executed w/ real outputs; native-runner-only legs named external gates, never faked); known stale spots from parked minors (architecture.md:56 mermaid FE node "Electron + React UI", AGENTS.md:44 browser-panel sentence); release-gating externals to carry into the checklist honestly (verified-apply #3, notifications:click #1, Win/Linux webdriver first runs, native perf gates).
Task 22: complete pending commit (report task-22-report.md; UNCOMMITTED per dispatch contract). Local matrix executed with real outputs: typechecks x4, Playwright 21/21, WDIO 12/12 (mac), parity 102, no-electron 15/15, tauri:build exit 0 (app+dmg), verify-tauri-artifacts 16 pass/0 fail/5 gates (testing mode, CI-shaped dist), go build/vet/test green, go test -race green on isolated rerun (first loaded run hit the known kilocode/opencode 3s-deadline flakes), flutter analyze clean + 1027 tests. Docs canonicalized across 26 files (AGENTS/CLAUDE twins, architecture mermaid FE node, development.md rewrite, STATUS.md frontend+in-flight rewrite, telemetry 3 code-verified fixes, cli/README browser+preview sections, daemon-environment implemented banner, stack.md, README preview card, baseline status section, 14 landing MDX files). Sweep fully classified (deliberate release-mandatory compat-feed chain kept; renderer comment residue listed file:line). Checklist: 8 PASS, 6 OPEN EXTERNAL GATE, 1 DEFERRED-WITH-RULING, 2 partial/rule. Concerns flagged for controller: npm run lint exit 1 (golangci v2.12.2: 108 findings = 92 generated-file false positives that vanish scoped + 16 real in agentbrowser + 4 G101 in generated codexproto; CI leg never ran on branch); shell-new-session-shortcut 4 vitest failures from UpdateOptInPrompt QueryClient (stash-proven pre-existing at HEAD); audit:tauri-state shutdown-phase timeout on darwin debug builds (phase0 leg trigger-disabled); agent-ci renamed upstream to run-local-ci.
Task 22: implementer DONE_WITH_CONCERNS (173 tool calls): 8 brief docs + desktop-release re-verified-no-change; sweep extended same-class to root CLAUDE.md, DESIGN.md, docs/cli+daemon-environment+stack, 14 landing MDX files; full local matrix green (typechecks x4, Playwright 21, WDIO 12, parity 102, no-electron 15, tauri:build exit0 app+dmg, verify-tauri-artifacts 16/0/5gates, go build/vet/test/-race green w/ isolated-rerun flake pair, flutter analyze clean + 1027 tests); checklist 17 items = 8 PASS / 6 OPEN EXTERNAL GATE / 1 DEFERRED-WITH-RULING / 2 partial-or-rule. Concerns flagged OUT-of-docs-scope for final review wave: (1) golangci-lint v2.12.2 exit1 — 92 sqlc-generated FPs (upstream exclusion inconsistency) + 16 real agentbrowser + 4 G101 generated codexproto; CI lint never ran on unpushed branch; (2) 4 vitest failures shell-new-session-shortcut.test.tsx — Task 20 residue, UpdateOptInPrompt useQuery lacks QueryClientProvider in that harness (stash-proven at HEAD); (3) audit:tauri-state darwin-debug shutdown timeout reproduced x2, its phase0 leg trigger-disabled; (4) @redwoodjs/agent-ci renamed run-local-ci. Controller ruling: concerns 1-4 are cross-cutting tooling/test debt recorded for the FINAL review's single fix wave — not Task 22 defects. Reviewer dispatched (opus, review-task22.diff).
Task 22 review verdict: SPEC mostly-compliant w/ one gap / QUALITY NEEDS FIXES — 0C/1I/3M. ~30 independent fact-checks all true (Go 1.25.7 go.mod, Rust 1.96.0 rust-toolchain.toml, Node 24 checks/22 build legs, Flutter 3.44.5, all documented commands exist, all cited code locations real incl. lib.rs:416 + fail-closed codes); matrix honesty honored w/ reconciling arithmetic; checklist integrity verified against ledger incl. live phase0-decision reproduction; no PASS without real artifact.
Task 22 Important T22-1: landing-doc browser-panel residue survives keyword sweep — dashboard.mdx:60-64 "Browser Preview" section + tab-profile-sharing + network-metadata capture presented as CURRENT UI; troubleshooting.mdx:77-87; examples.mdx:80-93; quickstart.mdx:78-86; cli.mdx:188 DIRECTLY CONTRADICTS rewritten docs/cli/README.md (devtools/capture now fail-closed); softer phrasing index.mdx:8 faq.mdx:8 dashboard.mdx:6,15 guides/parallel-issues.mdx:31,52. Live public docs steering users to a surface deleted in Task 21.
Task 22 minors (deferred): STATUS.md:3 branch-name framing flips stale on merge; checklist roll-up tally fuzzy vs item statuses; development.md "release/build legs on Node 22" over-broad (publish-feed/e2e-gate run Node 20).
Task 22: fix round 1/5 dispatched — RESUMED same implementer with T22-1.
Task 22: fix round 1/5 (1 addressed, 0 open — T22-1 landing browser-panel residue: dashboard/troubleshooting/examples/quickstart/cli.mdx live-UI claims rewritten to external-preview + fail-closed standalone-runtime reality, plus 4 same-class spots the broader filter surfaced (quickstart inspector line, platforms.mdx in-app browser inspector, architecture.mdx Bridges heading, quickstart heading); broader keyword sweep over landing docs now clean, residue classified deliberate; no test asserts on the content tree — fix report appended to task-22-report.md).
Task 22: fix round 1 implementer DONE: all 11 named T22-1 spots rewritten + 4 MORE same-class spots found by broader sweep (quickstart inspector line+heading, platforms.mdx in-app-browser-inspector, architecture.mdx Bridges heading) = 15 fixed; final sweep zero live browser-tab/panel instructions, 5 hits classified deliberate; DevTools/capture marked deferred via stable codes consistent w/ docs/cli/README.md + runtime.go:622-629. Scoped re-review dispatched (sonnet, review-task22-fix1.diff).
Task 22: fix round 1 re-review verdict: T22-1 ADDRESSED — all 11 named spots + 4 self-found verified spot-by-spot vs runtime.go:622-629/policy.go/cli/browser.go:238-243/install.go:114/useExternalPreview.ts facts; independent broader sweep reproduces exactly the 5 declared-deliberate hits; MDX tag balance verified.
Task 22: minor (deferred, round-1 re-review): index.mdx:21 "beside its Chat or terminal" survives keyword sweep (same-class residue; soft-misleading not dead-end; parallel-issues.mdx:52 twin WAS fixed) — one-line fix routed to final-review wave. Out-of-scope: runtime.go:632 daemon message still says "desktop browser panel runtime" (cosmetic wording drift, outside file set); migration.mdx:64 historical row correctly untouched.
Task 22: complete pending commit — loop closed after 1 fix round; 4 cross-cutting concerns + all parked minors routed to final whole-branch review triage.
Task 22: complete (commits 067aeff5c..9c8569eb5, review clean after 1 fix round; 30 docs files). ALL 22 TASKS COMMITTED. Next: final whole-branch review (merge-base 96c90c52d) with deferred-minors triage + 4 cross-cutting concerns.
