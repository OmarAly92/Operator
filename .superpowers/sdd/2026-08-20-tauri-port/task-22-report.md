# Task 22 report — final release gates and canonical documentation

Branch `codex/tauri-port`, base `067aeff5c`. All work UNCOMMITTED per contract override; nothing staged at any point; `frontend/src-tauri/gen/` and `frontend/src-tauri/target/` never staged or deleted. No production code changed — every diff below is documentation except where noted.

## 1. Per-document change summary

| File | Change |
| --- | --- |
| `AGENTS.md` | Commands section: replaced the removed `npm run build` with the real frontend gates (`typecheck`, `check:desktop-parity`, `node --test scripts/no-electron.test.mjs`) and added `npm run tauri:dev` / `npm run tauri:build`. Browser-panel demo sentence (stale since Task 16) replaced with external-preview wording incl. `opr preview clear`. Hard-rule citation fixed to `frontend/src-tauri/src/lib.rs` (parked Task 21 minor). `npx @redwoodjs/agent-ci` comment updated with the verified upstream rename to `run-local-ci`. |
| `CLAUDE.md` (root) | Twin browser-panel sentence fixed to the same external-preview wording (verified it still carried its own copy; Task 21 had fixed only `docs/CLAUDE.md`'s twin). |
| `README.md` | Dropped "browser preview in the inspector" phrase; replaced the "In-app browser preview" feature card (whose screenshot shows the removed panel) with an "External browser previews" card describing `opr preview`/`opr preview clear`. Old screenshot `docs/assets/readme/browser-preview.png` no longer referenced by any doc. |
| `docs/development.md` | Full rewrite: verified toolchain table (Go 1.25.7 per go.mod, Rust 1.96.0 per rust-toolchain.toml + CI `RUSTUP_TOOLCHAIN`, Node CI pins 24/22 with no engines pin, Flutter 3.44.5), Tauri project layout, `npm run tauri:dev` + sidecar prep, `tauri:build` platform artifacts, state roots, managed-browser first use, external preview, updater channels, the full local gate matrix, Phase 0/benchmark commands (every cited invocation executed during this task), artifact verification commands. Electron/Forge/Expo content removed. |
| `docs/STATUS.md` | Header and Build & test updated to Tauri reality; "Frontend (Electron + React)" section rewritten as "Frontend (Tauri + React)" (native integrations, parity ledger 102, standalone browser runtime, external preview, updater engine with the verified-apply fail-closed state named); "Mobile (Expo + React Native)" → "Mobile (Flutter)"; In-flight section now leads with the honest external-gate list. |
| `docs/architecture.md` | Mermaid System Overview nodes `FE[Electron + React UI]` / `Mobile[Expo + React Native UI]` → `FE[Tauri + React UI]` / `Mobile[Flutter Mobile UI]` (parked Task 21 minor + adjacent same-class staleness); "React Native" → Flutter client in the multi-listener section. |
| `docs/telemetry.md` | Three code-verified fixes: renderer is the Tauri webview; update-outcome events are decided in the Rust updater engine (`src-tauri/src/updater/`, automatic-failure suppression verified at `mod.rs:287-288`) not "Electron main / auto-updater.ts"; the daemon-env paragraph now states the verified truth — the Tauri supervisor stamps only `OPERATOR_TELEMETRY_APP_VERSION` + `OPERATOR_TELEMETRY_RENDERER` (`supervisor.rs:1130-1135`), never the sink toggles, so daemon capture is off in every launch path unless env is set. |
| `docs/cli/README.md` | `opr preview start` no longer "opens targets in the Browser panel"; `opr preview clear` documented. `opr browser` section rewritten from `backend/internal/adapters/agentbrowser/policy.go` + `runtime.go`: daemon-owned agent-browser engine, verified allowlist, `BROWSER_DEVTOOLS_UNAVAILABLE` / `BROWSER_AUTOMATION_UNAVAILABLE` fail-closed codes, no desktop window required, per-session profile teardown. |
| `docs/daemon-environment.md` | Status header flipped proposed→implemented with a "Where it lives now" banner pointing at `frontend/src-tauri/src/daemon/supervisor.rs` (`ensure_shell_env`) + `discovery.rs`; body explicitly marked historical design record. |
| `docs/stack.md` | Frontend-shell row: Electron + TypeScript → Tauri 2 + React/TypeScript with removal note. |
| `docs/benchmarks/tauri-port-baseline.md` | Appended "Status after Task 22 (2026-08-25)": locally runnable gates green; every Electron-comparison binding gate named open pending native runners; stop-port reasons stand; release-gating follow-ups (verified-apply, toast-click) and Win/Linux webdriver legs restated. |
| `frontend/docs/desktop-release.md` | No content change needed — already canonical from Task 18; facts re-verified during this task (scripts exist, Node 22.23.2 ACP runtime claim matches `build-acp-runtime.mjs:16`, no nightly schedule workflow exists, compat-feed Electron mentions are the deliberate release-mandatory chain). |
| `frontend/src/landing/content/docs/*.mdx` (14 files) | Live landing-site docs de-Electroned: architecture.mdx (system diagram, browser-bridge paragraph → standalone runtime, webview-data wording, Expo→Flutter), installation/index/faq ("Electron data" → desktop webview data), faq + notifiers ("Electron toasts" → native toasts), cli.mdx (Browser panel → external preview; Electron browser → daemon-owned runtime), migration.mdx, examples.mdx, terminals/web.mdx, plugins index rows, lifecycle-automation.mdx. Changelog MDX left historical. |

## 2. Command matrix — real outputs

Local host: darwin/arm64, Apple M1 Max, macOS 25.5.0. Toolchain: Node v22.23.2 / npm 10.9.8, Go go1.25.12 (module requires 1.25.7), rustc/cargo 1.96.0 (matches pin), Flutter 3.44.5 (matches CI pin).

| Command | Local result |
| --- | --- |
| `npm run lint` | **FAIL (exit 1), two runs, two different causes.** Run A (background, machine loaded): embedded `go test ./...` printed `FAIL` (package name lost to the script's `tail -25`; failure class matches the ledger's known load-sensitive auth-probe flakes). Run B (clean, full capture): `go test ./...` green (138 ok), then golangci-lint v2.12.2 (pinned, via `go run`) reported **108 issues**: 49 errcheck + 39 dupl in `internal/storage/sqlite/gen/*` and 4 gosec G101 in generated `codexproto/protocol.gen.go` — files that score **0 issues when those packages are linted scoped** (`golangci-lint run ./internal/storage/sqlite/gen/...` → "0 issues"), i.e. an upstream generated-file-exclusion inconsistency in full-module runs; plus **16 genuine findings in the new `internal/adapters/agentbrowser` package** (4 errcheck, 7 gocritic, 4 gosec G301/G302/G306, 1 nilerr). CI's golangci-lint leg has never run on this branch (branch unpushed; last go.yml success on master predates the port). **Flagged as a concern requiring a fix dispatch — not fixable inside a docs task** (gen/ is hands-off by hard rule; agentbrowser findings are a code round). |
| `npm run frontend:typecheck` | PASS, exit 0. |
| `cd frontend && npm run typecheck` | PASS, exit 0. |
| `npm run typecheck:e2e` | PASS, exit 0. |
| `npm run typecheck:e2e-tauri` | PASS, exit 0 (script exists; run as the WDIO suite's type gate). |
| `npm run test:e2e:renderer` | PASS — **21 passed** (17.5s), @T0/@P0 Playwright against dev:web. |
| `npm run test:e2e:tauri` | PASS — **12 passing** (32.6s), WebdriverIO against the real debug binary with the `e2e` Cargo feature (macOS leg). |
| `npm run check:desktop-parity` | PASS — "Desktop parity ledger covers 102 entries." |
| `node --test scripts/no-electron.test.mjs` | PASS — 15/15. |
| `npm run tauri:build` | PASS, exit 0 — `target/release/bundle/macos/Operator.app` + `Operator_0.10.3_aarch64.dmg` (cargo release 43.24s; unsigned base config). |
| `npm run verify:tauri-artifacts` | PASS after assembling the CI-shaped dist: `package-tauri-mac-zip.sh` (ditto zip, 77,427,029 bytes, sha256 93ab22f7…bbae6e) + dmg into one dir, then `--dist <dir> --mode testing` → **exit 0: 16 passed, 0 failed, 5 gated** (updater archives + codesign/spctl/staple recorded as GATEs — conductor material, correct for unsigned local builds). A naive first invocation against the raw `target/release/bundle` dir exited 1 (2 FAILs: zip/dmg not at dist top level) — user error, documented here so the CI layout (testing-build.yml "Collect bundle outputs") is understood as required. |
| `cd backend && go build ./...` | PASS, exit 0. |
| `go test ./...` | PASS, exit 0 (138 ok packages, 0 FAIL). |
| `go test -race ./...` | First run (concurrent with the cargo release build): 2 failures — `TestAuthStatusUnknownWhenKeyOnlyComesFromInteractiveShell` (kilocode) and `TestOpenCodeAuthStatusUnknownWithZeroCredentials` (opencode), both 3.00s context-deadline timeouts, the ledger's documented load-sensitive flake class. Isolated rerun on a quiet machine: **PASS, exit 0, 138 ok, zero failures.** |
| `go vet ./...` | PASS, exit 0. |
| `cd packages/mobile && flutter analyze` | PASS — "No issues found! (ran in 6.5s)". |
| `flutter test` | PASS — **+1027: All tests passed!** |
| `npx @redwoodjs/agent-ci run --all` | **NOT EXECUTED to completion — recorded honestly.** The package exists (registry v0.18.1) and AGENTS.md documents it, but invoking it prints "[Local CI] @redwoodjs/agent-ci has been renamed to the run-local-ci package" and re-exposes a `local-ci` CLI. A real `run --all` drives every PR/push workflow inside Linux Docker containers; this repo's decisive legs (macOS/Windows runners, signing, native WebDriver) cannot execute there, and reusable-workflow resolution needs a GitHub token. Local workflow validation therefore stays a partial smoke at best; the authoritative gate is CI on the pushed branch. AGENTS.md's comment line was updated to state the rename. |

### Supplementary runs taken as checklist evidence

| Command | Result |
| --- | --- |
| `npm run test` (vitest, after `npm ci --prefix src/landing` — the CI-documented landing install, which was missing locally) | 1712 passed / **4 failed** of 1716. The 4 failures are all `src/renderer/test/shell-new-session-shortcut.test.tsx` ("No QueryClient set" from `UpdateOptInPrompt.tsx:43`). **Proven pre-existing**: with this task's edits git-stashed, the same file fails identically (4 failed / 16 passed) on pristine HEAD — a Task 20 integration gap (the prompt's `useQuery` lacks a provider in that harness), surfaced only in full-suite runs. Left unfixed (code change, out of docs scope); flagged for a fix dispatch. |
| `node --test scripts/*.test.mjs` (all script suites) | 294 pass / 7 fail — the 7 (`blockmap`, `build-acp-runtime-helpers`, `feature-channel-resolution`, `feature-version`, `go-version`, `nightly-version`, `verify-mac-artifact`) are vitest-flavored suites; the complementary 16 are node:test-flavored and fail only vitest's bundler. Two test flavors, two runners; the union is green. Pre-existing repo condition, unchanged by this task. |
| `npm run test:tauri-state` | PASS — 17/17. |
| `npm run audit:tauri-state` | **FAIL locally, reproduced twice** (incl. after a fresh plain `cargo build`): "shutdown phase timed out" (30s) launching `target/debug/operator` in audit mode; app logs "global shortcuts unavailable on this platform; ignoring shortcut update" then hangs past the shutdown deadline. This is the Phase 0 workflow's per-OS evidence leg, which is trigger-disabled since Task 21. development.md documents the unit suite as the local gate and the audit as workflow-run. **Flagged as a concern** (native behavior, needs a native/CI run or a fix). |
| `node scripts/phase0-decision.mjs --results perf/results` | exit 1 — `stop-port`, naming every missing evidence file. Honest result preserved. |
| `npx -y @redwoodjs/agent-ci@0.18.1 --help` | Rename notice captured (see matrix). |

## 3. Stale-reference sweep — final classified state

Sweep: `rg -n "Electron|electron-forge|app://renderer|Browser panel|VITE_NO_ELECTRON" AGENTS.md README.md CLAUDE.md RUN_APP_COMMANDS.md CONTEXT.md CONTRIBUTING.md DESIGN.md docs frontend/docs frontend/src frontend/package.json .github/workflows` (excluding `docs/superpowers|plans|todo` historical trees and `frontend/src-tauri`). Every remaining hit classified:

### DELIBERATE / RELEASE-MANDATORY (kept)
- `frontend/docs/desktop-release.md:6,7,42,47,249` — Electron-compatibility YAML feeds and the installed-Electron-fleet migration are the release-mandatory chain (Task 21 "DO NOT REMOVE" ruling); :249 is an explicit "old Electron-side" historical contrast.
- `.github/workflows/frontend-release.yml:4,421`, `feature-release.yml:500`, `release-latest-guard.yml:5`, `mac-update-e2e.yml:113` — same compat-feed comments in the pipelines that produce them.
- `.github/workflows/testing-build.yml:9` — explicit "retired rather than ported" record of the deleted Electron pipeline.
- `.github/workflows/tauri-phase0.yml:3-6,337,345` — trigger-disabled retired workflow carrying the Task 21 RETIRED banner; body retirement was ruled Task 22-optional and deliberately left (the absence test enforces the disabled state).
- `docs/benchmarks/tauri-port-baseline.md` (17 hits) — Electron is the measurement baseline; the mentions ARE the contract.
- `docs/daemon-environment.md` (7) — dated design record with a new explicit "historical" banner.
- `docs/opr-start-bootstrapper-and-npm-deprecation.md:56,59,78,104` — dated 2026-06-26 implementation spec describing the Forge pipeline at deprecation time; historical record.
- `docs/STATUS.md:6,100,123`, `docs/development.md:5,224,242,267`, `docs/architecture.md:994`, `docs/cli/README.md:156`, `docs/stack.md`, `AGENTS.md:42` — explicit removal statements, gate names (`no-electron`), and "supports only the Electron shell" measurement-scope notes; intentional.
- `frontend/src/landing/content/changelog/operator-0-11-0.mdx:9` — changelog history.

### HISTORICAL NAMING RESIDUE — renderer code comments/tests (unchanged, classified)
Inert comments/test names referencing Electron provenance or the removed panel; no behavior defect, and the contract forbids code churn in this task: `frontend/src/shared/shortcuts.ts:2,7`; `shared/ui-locale.ts:1`; `shared/daemon-status.ts:2,25`; `styles/tokens.css:4`; `renderer/router.tsx:5`; `renderer/routes/_shell.tsx:427,486,805`; `renderer/hooks/useWindowFullScreen.ts:6`; `renderer/theme/token-map.generated.ts:241,243` (generated file — hands off); `renderer/lib/notifications.ts:264`, `notifications.test.ts:99`, `api-client.ts:213`, `api-client.test.ts:48`, `terminal-mux.ts:14`, `tauri-bridge.test.ts:302`, `event-transport.ts:40`, `daemon-telemetry.ts:1`, `bridge.test.ts:55,68,194` (contract tests over the shared bridge shape); `renderer/components/XtermTerminal.tsx:342,414`, `XtermTerminal.test.tsx:448,837`, `UpdateOptInPrompt.tsx:15,22`, `WindowTitlebar.tsx:85`, `chat/ElicitationCard.tsx:99`, `chat/CopyButton.tsx:35`, `chat/ChatWorkspace.tsx:152`; `renderer/styles.css:2148,3094,3318`. A later cosmetic rename pass can clean these; nothing behavioral hides in them (link-open tests assert `onLinkOpen` + no `window.open`, which is the shipped external behavior).

### FIXED this task
Everything else: the 26 files in section 1, including the two known parked spots (`architecture.md:56` mermaid node, `AGENTS.md:44` panel sentence), the root `CLAUDE.md` twin (confirmed still present, now fixed), and the landing MDX set the sweep exposed.

`app://renderer` and `VITE_NO_ELECTRON`: zero hits anywhere in the swept surface. (`app://renderer` does survive in live backend CORS defaults — `backend/internal/httpd/cors.go:79` / seeded `config.go:79` — parked as Task 21 minor #3; it is outside this sweep's file set and untouched.)

## 4. Final release checklist — item by item

1. **Phase 0 decision is `continue` or `linux-canvas`, with all native-runner evidence present** — **OPEN EXTERNAL GATE.** Recorded decision is `stop-port` (baseline doc §Phase 0 decision; reproduced live: `phase0-decision.mjs --results perf/results` exit 1 naming darwin/win32/linux evidence, app identity, updater signing). Proof needed: authorized-runner trust anchors (`phase0-release-trust.json` still deliberately unconfigured), signed native artifacts, all-platform evidence bundles.
2. **macOS webview and application state comply with the state-root rule** — **PASS (tests) with a named live-audit gap.** Webview pin verified at `frontend/src-tauri/src/lib.rs` (`.data_directory(state_root.join("webview"))`); `test:tauri-state` 17/17; WDIO launch-marker test writes under an isolated state root; relocation lock + state audits covered by `app_state_tests.rs` (17 named tests). Gap: the live per-OS audit (`audit:tauri-state`) times out in its shutdown phase on this host (§2 concerns) and its CI leg is trigger-disabled — first native workflow run owes the binding evidence.
3. **Packaged Tauri origins pass CORS and hostile origins fail before handlers execute** — **PASS (tests); live probe evidence external.** Exact-origin allowlist + preview-origin reorder pinned by `backend/internal/httpd/cors_test.go` (green in the full suite); the observed-response CORS probe (`phase0-cors-probe.mjs`) is CI-produced evidence, still owed from native runners.
4. **Terminal open, throughput, input, reconnect, active-memory, workload-CPU gates pass; macOS/Windows WebGL, Linux per recorded decision** — **OPEN EXTERNAL GATE.** No Electron baseline and no signed Tauri artifacts exist on this branch; `benchmark-shell.mjs` remains Electron-only and WKWebView exposes no measurement channel (baseline doc §Task 19 honest scope). Renderer code prefers WebGL with canvas fallback (`XtermTerminal.tsx:49,91`); Linux compositing decision awaits the Phase 0 verdict. Target numbers stand as written in the baseline binding-gates table.
5. **Warm/first-run startup, idle shell memory, base download, installed footprint meet all gates** — **OPEN EXTERNAL GATE.** Same measurement debt; targets: warm-start p50 ≤ 70% / p95 ≤ 75%, first-run faster at p50/p95, idle ≤ 60%, download ≤ 100 MiB and ≤ 70%, footprint ≤ 60%. Local route-graph (−34.5% parse bytes) and heap probes are checked in but explicitly non-binding.
6. **Base size includes daemon, agent-browser, ACP runtime; managed browser reported separately** — **PASS (structural, local).** Verifier confirms all three resources + licenses inside both the zip and dmg (16 PASS rows); managed-browser separation is enforced by the accounting contract and measured value remains part of gate 5's open evidence.
7. **Every non-browser parity-ledger row is implemented and tested** — **PASS with one DEFERRED-WITH-RULING row.** `check:desktop-parity` covers 102 live entries with archive reappearance rejection; WDIO 12/12 exercises the native seams end-to-end. The single open row `real-bundle-relocation-move` carries Task 20's explicit ruling (real signed-bundle ditto move proven by `app_state_tests.rs`; relaunch remainder marked OPEN honestly).
8. **`opr preview` automatically opens each new revision and supports manual reopen; clear opens nothing** — **PASS.** `useExternalPreview.test.tsx` pins exactly these behaviors ("auto-opens a new non-empty revision exactly once", "manual reopen validates HTTP(S), invokes the opener, and never acknowledges", "clear opens nothing", plus ack/restart/revision cases); daemon-side ack durability pinned by the sqlite integration test (Task 16 C1 fix).
9. **Standalone browser discovery/install/actions/session teardown work without Electron on every platform** — **PASS (tests) with platform caveats.** Daemon-owned agentbrowser adapter (install/policy/runtime suites green in the Go run), WDIO "answers standalone-browser route wiring through the daemon adapter", teardown wired into session destroy (`daemon.go`/`manager_test.go`). Windows paths remain uncompiled-locally (standing three-platform CI caveat); first Win/Linux CI runs are the proof obligation.
10. **Folder scans and telemetry bootstrap remain unreachable through the LAN listener** — **PASS.** Pre-auth prefix block + regression pins in `lan_listener_test.go` (green in the full suite); desktop-only routes 404 on LAN per the same tests.
11. **Flutter analyze/tests and authenticated mobile connection coverage pass; desktop-only routes 404 on LAN** — **PASS.** analyze clean, 1027 tests green; the mobile pairing/auth-probe order and LAN 404 coverage ride the suites above.
12. **App-state marker, install provenance, relocation/handoff, `opr start` discovery pass** — **PASS (tests) with the open relocation remainder.** `app_state_tests.rs` covers the marker/decision matrix/cross-process lock; `opr start` discovery reads `~/.operator/app-state.json` (`backend/internal/cli/start.go`); the relaunch remainder is the ledger's open row (item 7). Packaged-macOS relocation decision-gate run remains an external evidence item.
13. **Latest, nightly, feature, downgrade, return-home, and update telemetry flows pass signed E2E** — **OPEN EXTERNAL GATE (signed state).** Local unsigned exercise exists (Task 18: ephemeral-key signed archive crypto-verified, feeds generated, verifier structural passes; engine downgrade/recovery/opt-in covered by 52+ Rust updater tests and WDIO opt-in test). What's missing: real signed artifacts + published feeds on native runners (`mac-update-e2e.yml`, currently `--expect-stage-only` until verified-apply lands) and the update-outcome telemetry E2E over a signed install.
14. **The last Electron release migrates to Tauri via the compatibility feed or bridge release on every platform without losing `~/.operator` state** — **OPEN EXTERNAL GATE.** The compat YAML feeds are generated by the release-mandatory chain and point at Tauri artifacts by construction; the real migration E2E on all three platforms requires the signed releases of item 13. State preservation across migration is enforced by the state-root rule + relocation machinery but not yet exercised end-to-end.
15. **macOS zip and DMG, Windows NSIS, Linux AppImage/deb/rpm verify** — **PARTIAL: macOS PASS locally; Windows/Linux OPEN EXTERNAL.** Local: ditto zip + dmg built and structurally verified (16/16 applicable checks, 5 conductor-material gates recorded). Windows NSIS smoke and Linux AppImage/deb/rpm verification need their runners (`testing-build.yml` legs authored, unexecuted here).
16. **No renderer import, package, config, workflow, runtime token, or broker remains from Electron** — **PASS.** `no-electron.test.mjs` 15/15 (imports, packages, config, workflows, phase0 trigger-freedom); parity checker rejects archived-surface reappearance; Task 21 proved the lockfile removed 394 entries with 0 Electron survivors; `rg` sweep above shows only classified residue.
17. **Exactly one designated publisher performs the eventual release** — **PASS BY RULE (procedural).** `frontend/docs/desktop-release.md` "Hard rule: exactly one publisher" unchanged; approver list and release-environment gate documented; publication remains the designated conductor's separate action (this task publishes nothing).

## 5. Self-review

- **Honesty of the matrix**: every locally runnable brief command was executed with real captured output; the two failures (`npm run lint`, and the first `verify:tauri-artifacts` invocation) are reported with causes rather than smoothed over. `npx @redwoodjs/agent-ci run --all` was probed, not faked: the rename notice is quoted and the reason a full run is not meaningful locally is recorded. The `-race` flake pair is attributed with both the failing (loaded) and passing (isolated) runs shown. The vitest 4-failure set was stash-proven pre-existing on pristine HEAD before I claimed it as such.
- **Docs cite only verified facts**: every command written into `docs/development.md` was executed during this task (including discovering that `phase0-decision.mjs` requires `--results`, that `audit:tauri-state` does not pass locally — hence documented as workflow-run — and that `npm run build` no longer exists). Toolchain versions come from `go.mod`, `rust-toolchain.toml`, workflow pins, and the CI landing install — not memory. Telemetry and shell-env claims were verified against `supervisor.rs`, `desktop_telemetry.go`, `config.go`, and the Rust updater before writing them.
- **Scope discipline**: no production code changed; the only non-doc diffs are this report, the ledger line, and none besides. Generated dirs untouched; nothing staged; no commits made.
- **Known limitations of this round**: (a) the lint failure and the 4 pre-existing vitest failures and the `audit:tauri-state` local timeout are reported, not fixed — each needs its own small code/config dispatch; (b) renderer comment/test-name Electron residue is classified but not renamed (cosmetic, cross-cutting); (c) landing `node_modules` was installed locally to run the full vitest suite (untracked, matches CI's `npm ci --prefix src/landing`); (d) the README feature-card swap removes a stale screenshot reference — a fresh external-preview screenshot would improve marketing but cannot be produced honestly from this session.
- **Concerns for the controller** (also in my return message): golangci-lint v2.12.2 full-module run reports 108 findings (92 of them generated-file false positives that vanish in scoped runs — likely an upstream exclusion bug worth pinning in CI config; 16 real ones in `agentbrowser`); `shell-new-session-shortcut.test.tsx` 4 failures from `UpdateOptInPrompt`'s unprovided QueryClient (Task 20 residue); `audit:tauri-state` shutdown timeout on darwin debug builds; branch has never run CI.

## 6. Ledger line

Appended to `progress.md`: Task 22 complete pending commit — full local matrix executed with real outputs (all green except the classified lint/vitest/audit findings above), docs canonicalized across 26 files, sweep fully classified, checklist recorded with 8 PASS / 6 OPEN EXTERNAL GATE / 1 DEFERRED-WITH-RULING / 2 PARTIAL-or-rule items.

## Fix report round 1 (T22-1)

T22-1 accepted in full: the first sweep's keyword set (`Browser panel`, `Electron`, …) missed landing-doc residue that says "Browser tab" / "browser preview" / "network-metadata" / "DevTools" instead. Broader filters now applied over `frontend/src/landing/content/docs` (case-insensitive: `browser tab|browser panel|in-app browser|browser preview|browser bridge|network.metadata|devtools|WebContentsView|ephemeral|browser inspector|preview panel|tab profile`, changelog excluded).

### Fixed this round (11 coordinator-flagged spots + 4 same-class spots the broader filter then surfaced)

| Location | Fix |
| --- | --- |
| `dashboard.mdx` "## Browser Preview" section | Rewritten as "## Previews": external preview opens once in the system browser (`opr preview`/`opr preview start`/`opr preview clear`); browser automation is daemon-owned standalone runtime with per-session isolated Chromium profile discarded at session end; DevTools/network capture marked deferred via fail-closed `BROWSER_DEVTOOLS_UNAVAILABLE` / `BROWSER_AUTOMATION_UNAVAILABLE`. |
| `dashboard.mdx:6` | "…and browser preview" → "…and external previews". |
| `dashboard.mdx:15` (Inspector row) | Browser preview removed from the Inspector row (tab no longer exists). |
| `troubleshooting.mdx:77-87` | Section retitled "Previews And Browser Automation"; "The Browser tab is blank" → "The preview did not open" (external-open guidance incl. default-browser check and `opr preview clear` re-open); "targets the wrong page" accordion reworded to session-scoped daemon-owned automation (no "browser tab" selection). Server-won't-start accordion kept (still accurate). |
| `examples.mdx:80-93` | "Open the Browser tab in the session inspector" → "The target opens once in your system's default browser"; "When the Browser tab is attached…" → daemon-owned runtime, no window needed; network-metadata paragraph replaced with the fail-closed codes statement + profile teardown. |
| `quickstart.mdx:78-86` | "The Browser tab then displays the selected target" → "The selected target then opens once in your system's default browser"; heading "Try A Browser Preview" → "Try An External Preview". |
| `quickstart.mdx:49` (surfaced by broader filter) | Inspector list no longer claims "browser preview". |
| `cli.mdx:188-190` | Command-family list no longer claims DevTools or bounded network-metadata capture; explicit fail-closed codes sentence replaces the "network capture is off by default…" paragraph — now consistent with `docs/cli/README.md` and `backend/internal/adapters/agentbrowser/runtime.go:622-629`. |
| `index.mdx:8`, `faq.mdx:8` | "browser previews" → "external browser previews". |
| `guides/parallel-issues.mdx:31` | Table row "Browser preview / Dedicated selected-session browser target and ephemeral profile" → "External preview / A selected-session target that opens once in your default browser". |
| `guides/parallel-issues.mdx:52` | "…and browser preview" → `opr preview` opens a session's local app in your default browser. |
| `platforms.mdx:38` (surfaced by broader filter) | Linux paragraph no longer claims "the in-app browser inspector". |
| `architecture.mdx:75` (surfaced by broader filter) | Heading "Terminal And Browser Bridges" → "Terminal Streaming And Browser Automation" (body was already corrected in round 0). |

### DELIBERATE residue (final sweep output, justification per line)

```
docs/faq.mdx:8        — "external browser previews": accurate name of shipped behavior
docs/index.mdx:8      — "external browser previews": accurate name of shipped behavior
docs/dashboard.mdx:64 — deliberate deferral statement naming BROWSER_DEVTOOLS_UNAVAILABLE / BROWSER_AUTOMATION_UNAVAILABLE
docs/examples.mdx:93  — same deliberate deferral statement with the stable codes
docs/cli.mdx:190      — same deliberate deferral statement with the stable codes
```

Zero live-UI browser-tab/panel instructions remain. Changelog MDX and generated files untouched, as before.

### Test impact

No test asserts on these files' content: the only test reading the docs tree, `src/landing/scripts/generate-markdown-twins.test.mjs`, builds its own synthetic workspace (`createWorkspace()`/`writeDoc`) and validates the generator's parsing/rejection logic there — it never reads `content/docs/`. The generator itself runs at landing-build time over built `out/docs` HTML (requires a Next build; not runnable standalone — verified: `ENOENT out/docs`), and every edited file keeps its parsed structure intact (balanced `<Accordions>`/`<Accordion>` tags verified per file; the faq.mdx 0/1 artifact is the attributed opener `<Accordions expandAll>` not matching the bare-tag grep, confirmed balanced at lines 6/58). MDX content-only change; no test suite affected.

## Final fix wave report (single pre-merge dispatch, 2026-08-25)

Branch `codex/tauri-port`, HEAD `a47ed7c45`. One uncommitted wave carrying the complete approved-with-fixes list; nothing staged, no commits, `frontend/src-tauri/gen|target` untouched. golangci-lint v2.12.2 installed locally via `go install ...@v2.12.2` to run the gate for I1a/I1b/M3.

### Per-fix changes

| Fix | Files | Change |
| --- | --- | --- |
| I1a — deterministic lint exclusions | `backend/.golangci.yml` | Added `linters.exclusions.paths: [internal/storage/sqlite/gen/, codexproto/]`. A bare path-only rule under `exclusions.rules` is rejected by golangci-lint v2 ("at least 2 of (text, source, path[-except], linters) should be set"), so the whole-path mechanism is used instead — it excludes all linters for those generated trees and makes full-tree runs identical regardless of which packages the runner was pointed at. Cures the 92 gen findings (39 dupl + 53 errcheck) and the 4 G101 false positives on `"chatgptAuthTokens"` wire labels in `protocol.gen.go`. |
| I1b — 16 real agentbrowser findings | `backend/internal/adapters/agentbrowser/{install,runtime,policy}.go` | Enumerated from a fresh baseline run before fixing (108 total; 16 real). Perms: `install.go` EngineRoot MkdirAll 0o755→0o750, manifest WriteFile 0o644→0o600, `.install-tmp` MkdirAll 0o755→0o700; `runtime.go:494` Chmod keeps 0o700 with justified nolint (G302 assumes a file; target is the per-session state directory hardened to owner-only). errcheck two-value assertions: `runtime.go:696/:698` reuse the existing `tostring` helper behind the `isString` guards, `:746/:751` likewise. gocritic (7): `policy.go` two equalFold rewrites + `numberArg` paramTypeCombine; `runtime.go` `shapeActionValue`/`shapeTabClose` paramTypeCombine, if-else→type switch at the console/errors normalizer (with the string case binding), `randomHex(bytes int)`→`randomHex(size int)` importShadow. nilerr `runtime.go:389`: behavior unchanged, trailing `//nolint:nilerr // teardown succeeds when init already failed...` per house style (`session_manager/handoff_artifact.go` precedent). |
| I2 — shell shortcut harness | `frontend/src/renderer/test/shell-new-session-shortcut.test.tsx` | Mocked `UpdateOptInPrompt` as a null component alongside the existing NotificationCenter/CommandPalette/ShellTopbar one-liners (the file's established pattern); wrapping in QueryClientProvider would contradict the harness's documented no-provider stance (see its react-query mock comment). |
| index.mdx:21 | `frontend/src/landing/content/docs/index.mdx` | "Preview a session's local app beside its Chat or terminal." → "Preview a session's local app in your default browser." (mirrors `guides/parallel-issues.mdx:52`). |
| T20#3 — opt-in consent ordering | `frontend/src/renderer/components/UpdateOptInPrompt.tsx`, `UpdateOptInPrompt.test.tsx` | `answer()` now persists settings first and writes the localStorage asked-flag only after the set resolves; a failed set leaves both untouched so the ask retries next launch (console.warn retained). Tests rewritten to pin the new contract: success test asserts set-before-setItem via `invocationCallOrder`; new failure test pins flag-NOT-persisted on settings-write failure; former storage-blocked test inverted to pin choice-recorded-but-flag-unstored (re-ask next launch). |
| M2 string | `frontend/src-tauri/src/updater/mod.rs:1077` | → `"feature-build listing is unavailable in this shell: no GitHub releases transport is wired in yet"` — present-tense unavailable, no future-task promise. (`mod.rs:30` APPLY_DEFERRED_MESSAGE untouched: verified-apply genuinely remains deferred.) |
| M3 strings | `backend/internal/adapters/agentbrowser/runtime.go:635,:637` | Panel wording removed: "DevTools control is not available in the standalone browser runtime" / "This action is not available in the standalone browser runtime" — consistent with `docs/cli/README.md`'s standalone-runtime phrasing. Tests pin codes only, not messages (verified). |
| M4 prettier damage | `frontend/src/renderer/routes/_shell.tsx:680-687` | No prettier config exists anywhere in the repo and defaults would convert the file's tabs to spaces, so hand-fixed: removed the stray blank line after the `TerminalCacheProvider` opening tag and re-indented the orphaned "Shell chrome" comment to proper child depth with continuation alignment matching the UpdateOptInPrompt comment style directly above. The interior's shallower historical indentation predates the tauri branch (verified against upstream `5e50fc34c`) and was left unchurned. |
| STATUS.md sign-off ledger | `docs/STATUS.md` | New "Release sign-off ledger" bullet in In flight consolidating, factually: (a) Electron hidden/hiddenInset chrome divergence requires explicit user sign-off before any release (Task 13 ruling); (b) nightly channel has updater tooling but no schedule-triggered producer workflow on this branch (verified: only cleanup/guard workflows carry `schedule:`); (c) GitHub HTTPS transports unwired by design after the Task 17 TLS-surface ruling, stopped transports degrade safely, dedicated non-gating brief linked (tracked in git). |

### Validation (real outputs)

- `golangci-lint run ./...` (v2.12.2): exit **0**, output `0 issues.` — after `golangci-lint cache clean`, also warning-free (a stale-cache warning referencing the agent-orchestrator reference checkout appeared only with a warm cache shared by content-identical files).
- `go build ./...`: clean. `go test ./...`: exit **0** (138 package results, all ok).
- Frontend vitest (`npm test` = `vitest run --config vite.renderer.config.ts`): **1717/1717 tests pass, 0 failed**; both previously-failing files green (`shell-new-session-shortcut` 20/20, `UpdateOptInPrompt` 8/8). 16 *file-level* load errors remain, all `scripts/*.test.mjs` node:test suites vitest cannot bundle — proven pre-existing at HEAD `a47ed7c45` via a throwaway worktree run (HEAD: 24 files failed / **9 tests failed** = the exact 4+5 findings fixed here; wave: 16 files / 0 tests). Bare `npx vitest run` without the config fails identically.
- `npm run typecheck` (`tsc --noEmit`): exit **0**.
- `node --test scripts/no-electron.test.mjs`: exit **0**, 15 pass / 0 fail.
- `cargo fmt --check` (src-tauri): exit **0**. `cargo test --locked updater`: **56 passed, 0 failed**.

### Residue / concerns

- The 16 `scripts/*.test.mjs` vitest collection errors are branch-level pre-existing config drift (vitest exclude list covers node_modules/dist/e2e but not scripts/); their canonical runner is `node --test` via the package scripts. Not in this dispatch's fix list; left untouched.
- `_shell.tsx` interior JSX indentation inside TerminalCacheProvider remains at its pre-tauri depths (upstream-inherited); only the flagged ~680-684 region was restored.
