Implement the assign-and-edit half of the Planning Tickets feature (plan 3 of 3) by executing an existing implementation plan with subagent-driven development.

Read these first, in this order, end to end:
1. `AGENTS.md`, `CLAUDE.md` (the "clone agent-orchestrator verbatim" rule, the branch rule and the nested-worktrees warning are load-bearing) and `DESIGN.md`. The design reference `~/Projects/agent-orchestrator/packages/web/src` is a different app, not this repository.
2. `docs/superpowers/specs/2026-09-18-planning-tickets-design.md` (§2.4, §3.2, §3.3, §4, §5 and "Plan 3" in §6).
3. `docs/superpowers/plans/2026-09-18-planning-tickets-assign.md` — the plan you are executing: twelve tasks in file order 1 to 12, with full code, tests and commands. Its header section "Where the shipped code overrules the spec" is the truth when the spec and the daemon disagree: five assign warnings (`plan_order` included), `force` does not terminate the live session (the UI kills it first), the `{plan}` route segment is the bare file name, no dashed empty-lane style exists yet.
4. `docs/superpowers/plans/2026-09-18-planning-tickets-board-report.md`, "Planner review" — the three bugs green tests missed in plan 2 and the browser verification recipe you will reuse in Task 12.
5. `docs/superpowers/plans/2026-09-18-planning-tickets-daemon-report.md`, "Curl verification against a real daemon" — the curl shapes (loopback needs no bearer; `POST /api/v1/projects {"path"}` answers `{"project":{"id":"<folder name>",…}}`).

Baseline: `origin/development` at commit `809c3dde3` or later. Run `git fetch origin` first; plan 2 (board, ticket page, `useTicketsQuery`, `useTicketMutations` with `planParam`, `TicketRoleFields`, `PlanRow`, `TicketCard`, `TicketPage`) and the daemon are already merged there. Do not change anything under `backend/`; if the daemon genuinely lacks something, stop and report it.

Process:
- Use the superpowers `subagent-driven-development` skill: one fresh subagent per task, in plan order, with the plan's spec review and code-quality review between tasks. Do not start Task N+1 until Task N's gate passed and its commit exists.
- Work on a branch `feat/planning-tickets-assign` cut from `origin/development` inside a git worktree (superpowers `using-git-worktrees`). Put the worktree outside the checkout, at `../Operator-planning-tickets-assign`: worktrees inside `.worktrees/` or `.claude/worktrees/` poison repo-wide search (`CLAUDE.md`). Never commit to `development` directly and never touch `master`.
- Each task ends with its own conventional commit as written in the plan. End every commit message with the `Co-Authored-By` trailer your session's attribution reminder gives you.
- Task 1 runs the catalogue merge script from a file outside the repo (`/tmp/claude-501/add-ticket-keys.mjs`) and lands all 56 keys in all eight locales before any component work. Never touch the catalogues after a renderer or Tauri window is open.
- The plan's code is the reference, not gospel: if an existing signature differs from what the plan assumed (a dnd-kit listener type, `SettingsOptionMenu`'s trigger rendering for an empty value, `EditorView.findFromDOM` at the pinned version, a Testing Library query that matches two elements), adapt to the real code and say so in the task report. Do not change tests to make an implementation pass; fix the implementation. Do not relax a test's expected copy; the English strings come from the Task 1 catalogue. The plan names its only two allowed fallbacks (Task 5 keyboard drop in jsdom; Task 8 `findFromDOM`); take them only if the exact failure described happens, and record it.
- No comments in new code. Existing files keep the comments they have; `AgentModelField` moved in Task 11 keeps its comments.
- Keep every change inside the plan's file map plus whatever compile errors force. New dependencies are exactly the four CodeMirror packages in Task 8 at the pinned versions (or the newer patch `npm view` reports, recorded in the report). No other packages; dnd-kit is already installed.
- Never use `text-accent` or `bg-accent/*` (invisible in this theme). Nothing new renders a dialog inside `TicketCard`; the assign sheet is owned by `TicketDndProvider`. Drag handles and row actions stop propagation on click, pointerdown and keydown. Every new mutation that hits `/plans/{plan}/…` goes through `planParam` and has a test asserting the bare file name in `params.path.plan`.
- Every new component follows `DESIGN.md` and the agent-orchestrator look, built from `components/ui/*` primitives and the tokens the plan names. If you need a class the plan does not name, take it from a sibling component the plan cites (`ReviewPlanSheet`, `PlanRow`, `SplitLaneColumn`, `ConfirmDialog`, `SettingsRow`), not from memory.

Gates, run from `frontend/` per task, then once more at the end:
- `npm run typecheck && npm run frontend:lint && npx vitest run --config vite.renderer.config.ts`. Lint must show 0 errors; 150 pre-existing warnings are known.
- `npm run typecheck` rebuilds `packages/terminal` first. If it modifies `packages/terminal/package-lock.json`, `git checkout -- packages/terminal/package-lock.json` from the repo root before committing.
- The i18n gates are part of the vitest run: `src/renderer/i18n/renderer-coverage.test.ts` (no hardcoded English in JSX) and `src/renderer/i18n/instance.test.ts` (every locale has every key with matching `{{placeholders}}`).
- Green tests from a subagent are not proof. Run Task 12 yourself, not through a subagent, exactly as written: build `opr` from the worktree, start it isolated with `env -i HOME PATH` plus `OPERATOR_DATA_DIR`/`OPERATOR_RUN_FILE`/`OPERATOR_PORT=39312` (never the user's daemons on `3001` and `3002`; confirm both answer `/readyz` unchanged before and after), register the throwaway repo, seed the ticket with curl, serve the renderer with `vite --host --port 5180 --strictPort` and `OPERATOR_DEV_API_TARGET=http://127.0.0.1:39312` through a temporary `.claude/launch.json` entry, and open **`http://127.0.0.1:5180`** (not `localhost`: the daemon's CORS allow-origin is `tauri://localhost`, so only a same-origin `127.0.0.1` page proxies). Routes are hash routes: `#/projects/repo/tickets/search-page?file=plans%2F02-ui.md`. Drive the browser with the built-in browser tools; if `navigate` refuses the URL, `preview_start {url}` accepts it. Terminals show a demo terminal in this mode; everything else is real. Walk Steps 5 to 8 (dirty-warning drag assign, 409 → `Terminate and start` with the `-2` branch suffix, edit-save and both stale-bar paths, ticket defaults picked up by the planner spawn) and capture the request/status lines and curl outputs. Real agent sessions are spawned with `claude-haiku-4-5-20251001`; kill every one of them in Step 9, stop the daemon and Vite, and remove the temporary launch entry.
- Do not open or drive the Tauri window; the browser recipe replaces it.

Report:
- Write `docs/superpowers/plans/2026-09-18-planning-tickets-assign-report.md` as Task 12 Step 10 specifies: commit list, gate output summary, the CodeMirror versions installed, every deviation from the plan and why (including any fallback taken), the verification transcript from Steps 5 to 8, anything left undone with the reason. Commit it with a `docs:` message.
- Do not merge. Push the branch to `origin` (`git push -u origin feat/planning-tickets-assign`) and stop; a separate review session reviews, verifies on a real daemon and merges.
- Your final message: branch name, worktree path, HEAD sha, gate counts, which verification steps were completed in the real renderer, and what was not.

If a task is blocked (a plan assumption is wrong in a way you cannot resolve by reading the cited file, or a gate fails for a reason outside the task), stop and report the exact error and the file:line you were looking at rather than working around it.
