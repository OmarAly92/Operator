Write the implementation plan for plan 3 of 3 of the Planning Tickets feature: drag-to-assign, the ticket editor, and the project ticket defaults. Use the superpowers brainstorming skill only to confirm scope, then the writing-plans skill to produce the plan. Do not implement anything.

Read first, end to end, in this order:
1. `AGENTS.md`, `CLAUDE.md` (the "clone agent-orchestrator verbatim" banner and the branch rule) and `DESIGN.md`. The design reference `~/Projects/agent-orchestrator/packages/web/src` is a different app, not this repository.
2. `docs/superpowers/specs/2026-09-18-planning-tickets-design.md`, all sections; plan 3's scope is §2.4 (assign, dry run, warnings, `force`), §3.2 (drag and drop), §3.3 (the editing half: Edit / Preview / Split, save, dirty tracking, 409 bar, SSE reload only when clean), §4 (Reassign on a terminated plan, `ticket_not_on_default_branch`), §5 (the drag and editor Vitest bullets), §6 "Plan 3".
3. `docs/superpowers/plans/2026-09-18-planning-tickets-board.md` (plan 2, the code you extend) and `docs/superpowers/plans/2026-09-18-planning-tickets-board-report.md`, especially "Planner review": it lists three bugs the implementer's green tests did not catch and a browser verification recipe. Your plan must design around all of them (see "Lessons" below).
4. `docs/superpowers/plans/2026-09-18-planning-tickets-daemon-report.md`, "Planner review", for the daemon's shipped behaviour.
5. The shipped code, which is the truth over the spec where they differ:
   - daemon: `backend/internal/httpd/controllers/tickets.go` (`assign` at the `/plans/{plan}/assign` route, `writeFile`), `dto.go` (`AssignPlanRequest{harness, model, claudeAccountId, extra, force}`, `AssignPlanQuery{dryRun}`, `AssignPlanResponse{warnings, session?}`, `SaveTicketFileRequest{content, ifUnmodifiedSince}`, `TicketFileResponse{path, content, modifiedAt}`, `TicketDefaults{planner, implementer, reviewer: TicketRoleDefaults{agent, model, claudeAccountId}, reviewerMode, disableAutoReview}`), `backend/internal/service/ticket/service.go` (`assignWarnings`: the warning strings are `planning_active`, `plan_assigned`, `ticket_repo_dirty`, `ticket_not_on_default_branch`; `Assign` returns 200 + warnings on `dryRun`, 409 `TICKET_ASSIGN_BLOCKED` with `details.warnings` when warnings exist and `force` is false, 201 + session otherwise; `WriteFile` returns 409 `TICKET_FILE_STALE` with `details.modifiedAt` when `ifUnmodifiedSince` is older than the file), and the generated `frontend/src/api/schema.ts` for the exact TypeScript shapes.
   - frontend from plan 2: `frontend/src/renderer/hooks/useTicketsQuery.ts` (keys, `useTicketFileQuery`, live subscription), `hooks/useTicketMutations.ts` (`planParam`, `roleBody`, `ticketErrorMessage`), `lib/ticket-presentation.ts`, `lib/ticket-events.ts`, `components/tickets/*` (`TicketPage`, `TicketCard`, `PlanRow`, `TicketRoleFields`, `PlanWithAgentSheet`, `ReviewPlanSheet`, `MergeConfirmDialog`, `PlannedColumn`), `components/MarkdownBody.tsx`, `components/SessionsBoard.tsx` (the five-column grid, `WorkLaneColumn`/`SplitLaneColumn`, the sidebar is `components/Sidebar.tsx`), `components/ProjectSettingsForm.tsx` (how project config is read and saved with `PUT /api/v1/projects/{id}`), the i18n catalogues and their two tests.
   - `frontend/package.json`: `@dnd-kit/core`, `@dnd-kit/sortable`, `@dnd-kit/utilities` are installed and unused; no CodeMirror package is installed yet.

Baseline: `origin/development` at commit `c980f21fc` or later (plan 2 merged). Run `git fetch origin` first. Do not change anything under `backend/`; if the daemon genuinely lacks something plan 3 needs, write it down as a blocker in the plan's header instead of planning a backend change.

Scope of plan 3 (frontend only):
- **Drag to assign** (§3.2): dnd-kit. Draggable: plan rows on ticket cards whose status allows a new assignment (`todo`, `terminated`; `plan_assigned` warning otherwise per the daemon). Droppable: the IDLE / WORKING column and the sidebar project headers of the same project. While dragging, other columns dim and the target shows the dashed highlight already used for empty lanes (find it in `SessionsBoard.tsx`; if none exists, write "not known" and design one from the board's tokens). Keyboard-accessible per dnd-kit's sensors. Dropping opens the assign confirm sheet.
- **Assign confirm sheet**: read-only ticket, plan, project and branch name `opr/<slug>-NN` (derive NN with `planNumber`, and note that the daemon appends an attempt suffix on reassign; show what the daemon returns after the fact rather than guessing); editable harness, model, Claude account, extra via the existing `TicketRoleFields`; a dry run (`POST …/assign?dryRun=1`) runs when the sheet opens and its warnings show one per line with human copy for each of the four warning strings; `Start` (Enter submits, Escape cancels with nothing spawned); `plan_assigned` turns the button into `Terminate and start` and sends `force: true`; any other warning also requires `force: true` (the daemon blocks otherwise). Success navigates to the spawned session, whose `ticket` link the daemon already sets. The same sheet is also reachable without drag: an `Assign` action on todo rows and `Reassign` on terminated rows (§4), on both the card and the ticket page, so mobile-later and keyboard users have a path.
- **Editor** (§3.3 editing half): add `@codemirror/state`, `@codemirror/view`, `@codemirror/lang-markdown`, `@codemirror/commands` (pin versions you verify exist on npm; cite the versions), line wrapping, the terminal's mono font and the app skin's colours (find the font token in `TERMINAL.md`/`styles.css`, not from memory). Toolbar: file name, dirty dot, `Edit / Preview / Split` segmented control (the `settings-segment` RadioGroup pattern from `ReviewPlanSheet`), Save (Cmd+S / Ctrl+S). Save sends `PUT …/file` with `ifUnmodifiedSince` = the loaded file's `modifiedAt`. A 409 `TICKET_FILE_STALE` shows a bar with `Reload` and `Keep mine` (Keep mine re-saves without `ifUnmodifiedSince`). Files reload on the SSE change only when clean; when dirty the same bar appears. Split shows editor and preview side by side with the preview scroll following the editor's top line (state the exact mechanism; if you cannot make it deterministic, scope it to "preview scrolls to the heading nearest the editor's first visible line" and say so). The route search param `file` keeps working; navigating away with a dirty editor asks with `ConfirmDialog`.
- **Project ticket defaults UI**: a "Tickets" section in `ProjectSettingsForm.tsx` for `ProjectConfig.tickets`: planner / implementer / reviewer rows each with agent, model, Claude account (reuse the settings form's existing agent/model/account controls, cite them), `reviewerMode` (`planner` | `new`), `disableAutoReview`. Empty means inherit the project's worker defaults, which the daemon already does. Scratch projects show the same "not available" copy pattern the form uses for other repo-only sections.
- **Small carry-overs from plan 2's review**: a back-to-board crumb on the ticket page (the shell topbar is hidden on macOS for the ticket route, so the page itself needs one); the pre-existing intake-issue chip at `SessionsBoard.tsx` uses `text-accent`, which is invisible in this theme — do not use `text-accent` anywhere, and fix that chip only if the file map already touches that block.
Out of scope: mobile (plan 4 later), backend changes, any change to the review/merge flow shipped in plan 2.

Lessons from the plan-2 review that the plan must encode as constraints and tests:
1. The `{plan}` route segment is the bare file name (`01-daemon.md`), never `plans/01-daemon.md`; use `planParam` from `useTicketMutations.ts` for the assign mutation and add a unit test asserting the request path params.
2. React events bubble through portals: any dialog rendered inside `TicketCard` (`role="button"`) must sit inside the existing propagation-stopping wrapper, and drag handles must call `stopPropagation` so a drag start does not open the ticket. Tests for the card must assert `navigate` was not called after interacting with a dialog.
3. `text-accent` and `bg-accent/*` resolve to a 6 % white in this theme; use `text-foreground`, `text-muted-foreground`, the `text-status-*` tokens or bordered chips.
4. Every user-visible string goes through `t()` in all eight locales (`frontend/src/renderer/i18n/*.json`, tests `renderer-coverage.test.ts` and `instance.test.ts`), added in the plan's first task so the app is never relaunched after an i18n edit.
5. Green unit tests are not proof. The plan's final task must verify in the real renderer against a real isolated daemon using the recipe in the plan-2 report's "Planner review" (isolated `opr` build on a spare port with `env -i HOME PATH` and `OPERATOR_*` vars, `vite --host --port 5180` with `OPERATOR_DEV_API_TARGET`, open `http://127.0.0.1:5180`, hash routes `#/projects/<id>/...`), never the user's daemons on 3001/3002. Spell out the exact curl and browser steps for: a drag-drop assign with a `ticket_repo_dirty` warning shown and forced, a `plan_assigned` → `Terminate and start` path, an edit-save round trip, a stale save producing the 409 bar, and saving ticket defaults then seeing the planner spawn use them (check `GET /api/v1/sessions` for the model).

Contract facts to verify against the shipped code before relying on them: `assign` request/response shapes and status codes above; `writeFile` 409 details; `SessionTicketRef` on the spawned session; `ProjectConfig.tickets` persisted through the same `PUT /api/v1/projects/{id}` the settings form already uses (confirm the body shape in `ProjectSettingsForm.tsx` and `schema.ts`). Cite file:line for every anchor; write "not known" rather than guess.

Plan format: the writing-plans skill's format, saved as `docs/superpowers/plans/2026-09-18-planning-tickets-assign.md`, with the header (Goal, Architecture, Tech Stack, Spec path, Baseline commit), a Global Constraints section (branch `feat/planning-tickets-assign` from `development`, no comments in new code, DESIGN.md and shadcn primitives, the five lessons above, the frontend gates `cd frontend && npm run typecheck && npm run frontend:lint && npx vitest run --config vite.renderer.config.ts`, `git checkout -- packages/terminal/package-lock.json` if typecheck dirties it), a file map, and bite-sized TDD tasks (catalogue first, then presentation/helpers, dnd wiring, assign sheet, editor, split/preview, settings, crumb, final verification) each ending in a commit. Every task carries the actual test and component code, no placeholders. New dependencies are added in the task that first uses them with the exact `npm install` command and the version.

Before finishing, run the writing-plans self-review (spec coverage against §2.4, §3.2, §3.3 editing, §4, §5; placeholder scan; type consistency with plan 2's exported names) and commit the plan on `development` with a `docs:` message and the Co-Authored-By trailer your attribution reminder gives. Then reply with the plan path and a ten-line summary of the tasks; do not start implementing.
