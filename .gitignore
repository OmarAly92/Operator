# Node
node_modules/
.pnpm/
dist/
build/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Go
.go/
bin/
*.test
*.out
vendor/
# compiled daemon binary
/backend/backend
operator.yaml

# Backend runtime data artifacts (SQLite store + WAL, CDC event log).
# Created at OPERATOR_DATA_DIR (outside the repo by default); ignored here so a
# data dir pointed at the tree never gets committed.
*.db
*.db-shm
*.db-wal
session-events.jsonl
session-events.jsonl.*

# Operator local session state
.operator/

# Local scratch workspace (icon generation, throwaway scripts)
.cache/
frontend/.tanstack/

# Operator reviewer scratch output. The reviewer agent runs inside the worker's
# worktree; its review writeup must never be committed onto the worker branch.
/review.md

# Environment
.direnv/
.env
.env.*
!.env.example

# Editor / IDE
.vscode/
.idea/
*.swp
*~

# Local tooling (not for the team)
.codegraph/
.gocache/
.cursor/

# OS
.DS_Store
Thumbs.db

# Personal local overrides (not for the team)
.envrc.local

# playwright artifacts
frontend/test-results/
/output/

# built daemon binary copied into the frontend bundle dir
frontend/daemon/

# pinned native browser automation binary copied during package preparation
frontend/agent-browser/

# Locally-added agent skills (not for the team), except curated project skills
.agents/
!.agents/
.agents/*
!.agents/skills/
.agents/skills/*
!.agents/skills/bug-triage/
!.agents/skills/bug-triage/**
skills-lock.json

# Local pnpm artifacts
pnpm-lock.yaml

# Subagent-driven-development scratch (task briefs and reports)
.superpowers/

# Local git worktrees (created via superpowers:using-git-worktrees)
.worktrees/
worktrees/
